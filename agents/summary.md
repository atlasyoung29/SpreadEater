# SpreadEater - Project Summary

## Recent Work
- 2026-04-25: completed a full local + live validation pass for the current sweep-mitigation branch. Validation passed for `cargo check --workspace --all-targets`, `cargo test --workspace --all-targets -- --nocapture`, `cargo run --quiet -- auth-check`, and the monitor web `npm run build`; the only skipped suite remains the ignored monitor Postgres integration set because Docker Desktop / local Postgres was unavailable. Live scenario refreshes were needed before the armed probes: `<repo-root>/fixtures/hedge_live_probe_scenarios/scotus_mail_ballots_buy_probe_5.json` now matches the current `0.70` trigger limit and `$3.50` trigger notional cap, `<repo-root>/fixtures/hedge_live_probe_scenarios/scotus_mail_ballots_yes_bid_probe_under_10.json` now has enough hedge-notional headroom for the current NO book, and the stale/non-sampling SCOTUS merge probe was replaced with `<repo-root>/fixtures/merge_live_probe_scenarios/anthropic_ipo_before_2027_pair_merge_probe.json`. User-stream smoke passed for the three hedge live fixtures, armed hedge probes passed for those three with `meta_pass=true`, `standard_pass=true`, and `cleanup_pass=true`, and armed merge probes passed for no-Fed, Playboi Carti, and Anthropic with `meta_pass=true`, `standard_pass=true`, and `cleanup_pass=true`. The old SCOTUS merge probe failed once before replacement because it acquired `5.007` paired shares and did not satisfy the strict standard verdict, but cleanup passed. `<repo-root>/HEDGE_TESTING_SUITE.md` was also corrected because the old standalone `cargo run -- hedge-test` / `cargo run -- hedge-replay` examples are no longer exposed by the current binary. Final read-only account audit reported `0` open orders and `0` active positions. Confidence the current branch is green across available offline, authenticated, and live-money validation surfaces: **high**. Confidence the remaining unrun risk is limited to the unavailable Postgres-backed monitor integration suite rather than core trading behavior: **high**.
- 2026-04-22: added two narrow sweep-mitigation runtime changes without changing the bot's hedge-vs-sellback planner or redefining unsafe conditions. In `<repo-root>/src/runtime/live_engine.rs`, BUY-side resolution prep now reclaims external resting bid capital when the filled market has already been cleaned up but free hedge budget is still below residual exposure size: it cancels bid legs on other markets, waits through the same bounded cancel window, refreshes balance again, and then feeds the reclaimed `max_hedge_usdc` into the existing resolution planner. The same file also now runs the existing hedge-depth guard immediately for the affected managed market on fresh market-book websocket `Snapshot`/`Delta` events, reusing the same `min_outcome_price`, `min_size`, and hedgeable-depth predicates/actions as the 2-second hedge-depth pass instead of inventing a new stress policy. `<repo-root>/src/trading/order_manager.rs` gained the supporting external-bid cancellation/count helpers, `<repo-root>/STRATEGY.md` was updated to describe the earlier hedge-depth timing and BUY-side reclaim behavior, and inline runtime regressions now cover reclaim/no-reclaim and websocket fast-path resize/cancel/no-op cases. Validation on 2026-04-22: `cargo test --bin spreadeater --no-run`, `cargo test --bin spreadeater prepare_market_for_resolution_ -- --nocapture`, `cargo test --bin spreadeater ws_hedge_depth_guard_ -- --nocapture`, and `cargo test --bin spreadeater hedge_depth_resize_emits_diagnostics -- --nocapture` all passed. Confidence the resolution change materially improves the chance of getting a real hedge-vs-sellback choice during BUY-side sweep incidents: **high**. Confidence the websocket fast path preserves existing unsafe predicates while materially shrinking the stale window between a book deterioration and the bot's first defensive action: **high**.
- 2026-04-22: corrected Reward Benchmarking v1 advisory metric weighting in `<repo-root>/scripts/summarize_benchmark.py` after PR review found that order/fill transitions were only taking effect on the next `status_snapshot`. The summarizer now advances the active snapshot clock to each `order_submitted`, `fill_detected`, `order_cancelled`, and `order_resized` timestamp before mutating order state, then recomputes reward-bid exposure utilization and live-order counts in-place so share-hours and live-order-hours reflect the actual event time. This fixes the advisory benchmark fields that depend on those integrals: `mean_bid_exposure_utilization_pct`, `reward_bid_exposure_share_hours`, `reward_bid_order_hours`, and `bid_churn_per_live_bid_order_hour`. Updated `<repo-root>/tests/test_benchmarking_scripts.py` so the partial-fill case, cancel/replace churn case, and end-to-end fixture now assert the event-accurate values instead of the previous snapshot-lagged expectations. Validation on 2026-04-22: `python -m unittest discover -s tests -p "test_benchmarking_scripts.py"` passed. `STRATEGY.md` remains aligned; no update was required because this is offline benchmark math only. Confidence the reviewer’s metric-trustworthiness concern was valid: **high**. Confidence the current summarizer now time-weights these advisory metrics at the correct event boundary: **high**.
- 2026-04-21: hardened Reward Benchmarking v1 auto-summarization after review found a stale-pointer risk in `<repo-root>/scripts/kill_flatten.py`. Auto-resolution through `data/current_run.json` now requires that the file contain a valid PID for a live `spreadeater` process before `--summarize-run` will attach a manual reward delta; if the PID is stale or malformed, the script warns and skips summarization unless `--run-id` is provided explicitly. Added Python regressions in `<repo-root>/tests/test_benchmarking_scripts.py` for both the live-PID happy path and the stale-PID skip path. In the same follow-up, `<repo-root>/.gitignore` was corrected so `data/events/**/benchmark_summary.json` remains trackable as durable team benchmark output, while `data/events/**/run_metadata.json` is ignored again because it contains machine-local paths and PIDs; the branch’s accidental raw/live artifact churn was also removed from version control scope. Validation on 2026-04-21: `python -m unittest discover -s tests -p "test_benchmarking_scripts.py"` passed, and `git check-ignore -v data\\events\\run_20260422_001400\\run_metadata.json data\\events\\run_20260422_001400\\benchmark_summary.json` confirmed that only `run_metadata.json` is ignored. `STRATEGY.md` remains aligned; no update was required because this pass changes benchmark operational safety and repository hygiene only. Confidence the stale-run misattribution path is closed for the normal `--summarize-run` flow: **high**. Confidence the benchmark artifact policy now better matches the intended team-sharing boundary: **high**.
- 2026-04-21: narrowed `.gitignore` so Reward Benchmarking v1 artifacts can be committed without admitting raw event dumps into git. `<repo-root>/.gitignore` now keeps `/data/*` ignored by default, reopens only `data/events/` traversal, and explicitly ignores `data/events/**/events.jsonl` and `data/events/**/events.jsonl.gz`. This leaves per-run `run_metadata.json` and `benchmark_summary.json` trackable while keeping raw JSONL payloads suppressed, and `data/current_run.json` remains ignored to avoid workspace noise from the active-run pointer. Validation on 2026-04-21: `git check-ignore -v data\\events\\run_20260422_001400\\events.jsonl data\\events\\run_20260422_001400\\run_metadata.json data\\events\\run_20260422_001400\\benchmark_summary.json data\\current_run.json` confirmed that `events.jsonl` and `current_run.json` are ignored while `run_metadata.json` and `benchmark_summary.json` are not. `STRATEGY.md` remains aligned; no update was required because this is repository housekeeping only. Confidence the ignore rules now match the intended benchmark artifact policy: **high**.
- 2026-04-21: fixed the first live Reward Benchmarking v1 parsing failure on real run metadata. `<repo-root>/scripts/summarize_benchmark.py` previously used Python 3.10 `datetime.fromisoformat()` directly, which rejected the nanosecond-precision `started_at` timestamps emitted by Rust/Chrono into `data/current_run.json` and `run_metadata.json` (for example `2026-04-22T00:06:27.102274200Z`). The summarizer now normalizes fractional-second precision down to microseconds before parsing, so Rust-style `Z` / offset timestamps with more than 6 fractional digits are accepted without changing runtime metadata format. Added a regression in `<repo-root>/tests/test_benchmarking_scripts.py` that feeds a nanosecond `started_at` through the real summarizer path. Validation on 2026-04-21: `python -m unittest discover -s tests -p "test_benchmarking_scripts.py"` passed, and `python scripts/summarize_benchmark.py --run-id run_20260422_000627 --reward-delta-usd 0.01` now succeeds against the user’s previously failing live run. `STRATEGY.md` remains aligned; no update was required because this is benchmark-tooling parsing only. Confidence the reported failure was fully addressed: **high**.
- 2026-04-19: added Reward Benchmarking v1 with a startup-only runtime metadata write and fully offline post-run analysis so live builds can be compared on actual reward yield without adding in-process aggregation overhead. `<repo-root>/src/runtime/run_metadata.rs` now canonical-hashes the loaded `Config`, resolves absolute run paths, and writes identical metadata into `data/current_run.json` and `data/events/<run_id>/run_metadata.json`. `<repo-root>/src/runtime/live_engine.rs` now records that metadata once during `LiveEngine::new(...)`, using the same generated `run_id` and the caller-supplied config path; no benchmark math runs in the bot loop. `<repo-root>/scripts/summarize_benchmark.py` streams a run’s `events.jsonl` line-by-line, rebuilds reward-bid order state from raw `order_submitted` / `fill_detected` / `order_cancelled` / `order_resized` events, computes the agreed benchmark fields into `benchmark_summary.json`, and keeps actual manual reward delta per hour as the primary truth metric. `<repo-root>/scripts/compare_benchmarks.py` now compares candidate vs baseline summaries with `pass` / `warn` / `fail` / `incomplete` verdicts driven only by `actual_reward_usd_per_hour`, while secondary utilization / churn / estimated-reward posture metrics remain advisory. `<repo-root>/scripts/kill_flatten.py` was refactored into a non-interactive CLI, can now resolve the active run from `data/current_run.json` before killing the bot, and can optionally invoke the summarizer after kill/cancel/flatten completes. Added focused runtime coverage for metadata emission in `<repo-root>/src/runtime/live_engine.rs`, plus Python benchmark-script coverage and a realistic mini JSONL fixture in `<repo-root>/tests/test_benchmarking_scripts.py` and `<repo-root>/tests/fixtures/benchmarking/mini_events.jsonl`. Validation on 2026-04-19: `python -m unittest discover -s tests -p "test_benchmarking_scripts.py"`, `cargo test --bin spreadeater startup_writes_run_metadata_files_with_expected_schema -- --nocapture`, `cargo test --bin spreadeater status_snapshot_emits_last_book_ws_stats -- --nocapture`, `cargo test --lib config_hash_is_stable_for_equivalent_configs -- --nocapture`, and `cargo test --bin spreadeater --no-run` all passed. `STRATEGY.md` remains aligned; no update was required because this pass does not change market selection, sizing, hedge behavior, or monitoring-side hot-path behavior. Confidence the new benchmark flow captures the agreed v1 metrics without adding meaningful main-bot runtime overhead beyond a single startup metadata write: **high**. Confidence the post-run summarizer and comparator are using raw JSONL truth rather than monitor/Postgres projections as requested: **high**. Confidence the current `kill_flatten.py --summarize-run` path resolves the intended run id before cleanup and can attach manual reward deltas to the exact run that just ended: **high**.
- 2026-04-17: hardened the gasless SAFE merge path so the hedge harness can pass on legitimate production behavior instead of falling over on transient relayer failures. In `<repo-root>/src/trading/ctf_merge.rs`, SAFE deployment / nonce / submit / lookup calls now do bounded retries on transport timeouts and transient relayer `408/429/5xx` responses, lookup timeouts no longer abort the existing 60-second relayer poll window, and terminal on-chain SAFE `STATE_FAILED` executions now retry with a fresh nonce up to 2 additional times before the runtime falls back to inventory asks. Standard markets still skip ERC-1155 approval entirely, while neg-risk approval remains on the CTF ERC-1155 contract for the adapter and now benefits from the same SAFE retry handling. Because the old checked-in standard merge probe market had become unreliable as a live validation candidate, the standard merge scenario was refreshed from the stale China market to `<repo-root>/fixtures/merge_live_probe_scenarios/playboi_carti_before_gta6_pair_merge_probe.json`. The Layer 2 hedge replay expectations in `<repo-root>/tests/support/hedge/layer2.rs` were also updated to match current intended behavior: when merge is unconfigured or fails after a split hedge+sellback resolution, the paired residual now exits through fallback inventory asks on both sides, exactly as `STRATEGY.md` describes. Validation on 2026-04-17: `cargo test --test core_types ctf_merge_ -- --nocapture`, `cargo test --bin spreadeater layer2_ -- --nocapture`, `cargo test --bin spreadeater -- --nocapture`, `cargo test --workspace --no-run`, `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_user_stream_smoke.ps1 -Scenario no_fed_rate_cuts_2026_yes_bid_probe_under_10`, `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_hedge_live_probe.ps1 -Scenario no_fed_rate_cuts_2026_yes_bid_probe_under_10`, `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_merge_live_probe.ps1 -Scenario no_fed_rate_cuts_2026_pair_merge_probe`, `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_merge_live_probe.ps1 -Scenario playboi_carti_before_gta6_pair_merge_probe`, and `python scripts/kill_flatten.py` all finished green on the final pass. Confidence the hedge harness is green on real production behavior rather than probe weakening: **high**. Confidence the remaining merge instability is now bounded to transient relayer/service conditions that the runtime handles with retries before resorting to fallback asks: **high**.
- 2026-04-17: hardened four stabilization paths on `feature/control-plane-productivity-followup` after the long-run review exposed remaining truth-gaps between exchange state, user-stream attribution, and calibration feedback. In `<repo-root>/src/runtime/live_engine.rs`, calibration prediction now uses order-level current-quote compatibility plus a current-multiplier score proxy instead of the older market-level `would_trade` shortcut, and unchanged orders that were just observed as non-scoring are temporarily treated as predicted non-scoring so the multiplier cannot keep ratcheting upward on the same false-positive sample. In `<repo-root>/src/trading/client.rs`, non-success scoring responses now surface as errors so failed probes are skipped rather than counted as "not scoring." The fill path now also has a bounded exact-signature fallback for unattributed user-stream trades against active or recently cancelled tracked orders, and verified self-executed sellbacks are recorded briefly so late websocket echoes are ignored instead of reopening an already-resolved market. Finally, both resolution prep and halted-market cleanup now reconcile market-scoped open-order truth and retry pending cancels before computing hedge affordability or deferring cleanup, which prevents stale tracked bids from pinning available hedge budget at zero and prevents halted markets from getting stuck behind phantom pending orders. Added targeted inline runtime regressions for all of those cases and updated `<repo-root>/STRATEGY.md` accordingly. Validation on 2026-04-17: `cargo test --bin spreadeater sample_order_scoring -- --nocapture`, `cargo test --bin spreadeater predict_order_scoring_uses_recent_actual_false_for_unchanged_order -- --nocapture`, `cargo test --bin spreadeater exact_signature_fallback -- --nocapture`, `cargo test --bin spreadeater recent_resolution_trade_skip -- --nocapture`, `cargo test --bin spreadeater finalize_halted_cleanup_reconciles -- --nocapture`, `cargo test --bin spreadeater prepare_market_for_resolution_prunes_stale_orders_before_budget -- --nocapture`, `cargo test --bin spreadeater --no-run`, `cargo test --bin spreadeater -- --nocapture`, and `cargo test --workspace --no-run` all passed. Confidence the calibration loop no longer has the observed "same false positive forever" ratchet on unchanged orders: **high**. Confidence the stale-order truth fixes address both the Starmer-style deferred cleanup wedge and the observed zero-hedge-budget misclassification without adding hot-path strategy overhead outside rare resolution/cleanup paths: **high**. Confidence the exact-signature fallback plus late-sellback suppression materially reduces missed or double-counted fill handling without broadening the bot into unsafe guess-based attribution: **high**.
- 2026-04-16: added paired neg-risk merge support on `feature/neg-risk-merge-safety` without broadening the inventory model. In `<repo-root>/src/trading/ctf_merge.rs`, pair merges now route explicitly by market class: standard markets still submit `mergePositions(...)` to the CTF contract, while `neg_risk=true` markets submit that same paired-merge calldata to the Neg Risk Adapter. SAFE approval remains on the CTF ERC-1155 contract, but the approved operator now matches the selected venue, and approval caches are split between standard and neg-risk so the first neg-risk merge cannot be skipped behind a prior standard approval. In `<repo-root>/src/runtime/live_engine.rs`, pair exits now resolve merge venue from `CanonicalMarket.neg_risk` and refuse to guess when market metadata is missing: production records a clear merge-failure reason and falls back to the existing ask/idle behavior, while the harness fails fast so scripted validation cannot silently assume standard semantics. Added regression coverage in `<repo-root>/tests/unit/trading/ctf_merge_tests.rs` and inline runtime tests for standard routing, neg-risk routing, independent approval caches, missing-metadata failure, and runtime refusal to guess a venue. Validation on 2026-04-16: `cargo test --test core_types ctf_merge_ -- --nocapture`, `cargo test --bin spreadeater harness_merge_pairs_ -- --nocapture`, and `cargo test --bin spreadeater merge_ -- --nocapture` all passed. Confidence paired neg-risk merge routing is now correctly wired for the bot's YES+NO single-question exit path: **high**. Confidence the missing-metadata guard removes the old silent standard-merge assumption without changing outward fallback semantics: **high**. Confidence this does not add meaningful main-bot performance overhead because the new logic runs only on the rare pair-exit path and the approval split is a constant-time venue branch: **high**.
- 2026-04-16: refreshed the checked-in SCOTUS standard hedge live-probe fixture at `<repo-root>/fixtures/hedge_live_probe_scenarios/scotus_mail_ballots_yes_bid_probe_under_10.json` after the full live matrix found that the stored trigger cap had gone stale. The fixture now uses the live-safe trigger cap that passed on 2026-04-16 (`0.96` max trigger limit, `4.80` max trigger notional, `5.50` cleanup cap) and its description/sellback allowance now match the current buy-side hedge-or-sellback production path. Re-running the repo fixture after the refresh passed with `meta_pass=true`, `standard_pass=true`, and `cleanup_pass=true`, so the full scripted live matrix is green again without a temp override. Confidence this was fixture drift rather than a bot regression: **high**.
- 2026-04-16: fixed two stabilization bugs on `feature/control-plane-productivity-followup` without changing strategy policy or public config. First, authenticated user-WS liveness now reflects any real socket activity, not just parsed Trade/Order events: `<repo-root>/src/trading/user_stream.rs` now emits an internal `UserEvent::RawActivity` for `PING`, `PONG`, and non-business text frames, `<repo-root>/src/runtime/live_engine.rs` treats that as watchdog-only liveness, and `<repo-root>/src/watchdog/health.rs` resets the silence timer without creating fill/order side effects. Second, calibration’s scoring prediction in `<repo-root>/src/runtime/live_engine.rs` now uses the same fresh-book, competition-adjusted evaluation path as live admission instead of the old hardcoded/geometry-only assumption, and sampled orders predict `true` only when the market is still viable and the tracked order remains compatible with a currently approved quote. Added regression coverage for raw user-WS liveness, multiplier-sensitive prediction flips, and the false-positive calibration loop stopping after a multiplier increase. Validation on 2026-04-16: focused watchdog/calibration slices passed, and `cargo test --bin spreadeater -- --nocapture` passed with `308` passed, `0` failed, `3` ignored. Confidence the false user-WS silence classification is fixed: **high**. Confidence the calibration loop can now legitimately converge because multiplier changes feed back into future predictions: **high**. Confidence there is no meaningful main-bot performance overhead beyond a tiny watchdog-only event on existing raw user-stream frames and the already-scheduled low-rate calibration sample path using the canonical evaluator: **high**.
- 2026-04-15: hardened the live validation harness and user-stream connector without changing strategy behavior. In `<repo-root>/src/trading/user_stream.rs`, the user websocket now authenticates with the documented auth message, sends an immediate heartbeat, and treats the first heartbeat/data frame as a successful connection instead of waiting for an ACK the venue never sends; the connect-only smoke probe now round-trips `PONG` in under a second. In `<repo-root>/tests/support/hedge/live_probe.rs`, both armed live probes now derive marketable BUY limits from the current live asks plus one tick and fail clearly if those real prices exceed the scenario safety caps, instead of placing non-marketable orders and later reporting misleading “missing hedge decision” or “missing pair” failures. The cleanup observer used by those probes also now gets a small dedicated post-timeout cleanup window and recognizes a stable flat baseline that lands exactly on the timeout boundary. In `<repo-root>/src/trading/client.rs`, `get_order()` now treats a venue `null` order payload as a missing order instead of a hard deserialization failure, which unblocked the merge live probe when acquisition orders disappeared quickly after execution. Refreshed the two SCOTUS manual live-probe fixtures to current safe caps, added unit coverage for the user-stream auth shape, heartbeat detection, marketable-limit derivation, cleanup timeout floor, and null order parsing, and reran the full live validation battery. Validation on 2026-04-15: `cargo test --bin spreadeater -- --nocapture` passed with `304` passed, `0` failed, `3` ignored; `cargo test --bin spreadeater hedge_harness -- --nocapture` and `cargo test --bin spreadeater harness_merge_pairs_ -- --nocapture` both passed; `run_user_stream_smoke.ps1`, `run_hedge_live_probe.ps1`, and `run_merge_live_probe.ps1` all finished green with `meta_pass=true`, `standard_pass=true`, and `cleanup_pass=true` on the armed probes. Confidence the websocket handshake issue is resolved: **high**. Confidence the live probes now fail truthfully on stale caps and pass on legitimate live fills/merges: **high**. Confidence no trading-strategy overhead was added because the changes are connector/probe-side only: **high**.
- 2026-04-14: fixed three live-run productivity failures in `<repo-root>/src/runtime/live_engine.rs` without adding hot-path strategy overhead. First, stale-book halts no longer spam repeated risk-state transitions: duplicate stale-book kills are suppressed, halted-market cleanup state is deduplicated, and stale-book halts now auto-resume only after cleanup verifies flat/no-orders plus two consecutive fresh-book confirmations. Second, score-proxy calibration no longer assumes every tracked order should be scoring; calibration samples now predict scoring from the order's real market metadata, tracked price/size, and fresh current books via `per_order_score(...)`, skipping stale or missing book truth instead of ratcheting the multiplier upward on false positives. Third, approved bid candidates are now clamped to currently hedgeable opposite-book depth before placement, and both admission-time clamping and periodic hedge-depth resizing now floor to whole shares instead of rounding up above actually hedgeable depth. Added regressions for duplicate stale-book halt suppression, stale-book auto-resume / streak reset, truthful calibration sampling, stale-book sample skipping, fractional-depth admission clamping, and floor-preserving hedge-depth no-op behavior. Updated `<repo-root>/STRATEGY.md` Sections 2.2, 3.1, and 6.5 plus agent docs. Validation on 2026-04-14: `cargo test --bin spreadeater -- --nocapture` passed with `298` passed, `0` failed, `3` ignored after the focused stale-book / hedge-depth / calibration slices also passed. Confidence the stale-book churn fix addresses the observed halt spam without weakening risk controls: **high**. Confidence the calibration fix closes the observed multiplier drift caused by always predicting `true`: **high**. Confidence the depth clamp fix removes the admission-time overshoot and the whole-share floor prevents reintroducing a one-share oversize edge case: **high**.
- 2026-04-13: fixed the merge `standard_pass` failure mode by separating economic merge success from delayed post-merge visibility. In `<repo-root>/src/runtime/live_engine.rs`, confirmed relayer merges now compute the expected post-merge position, refresh balance, do one immediate position sync, and then reuse a shared merge-truth observer against the direct `/positions` source. Production spawns that observer in a detached task so fill handling and the per-market hedge lock are not held open by visibility lag; on timeout it emits a degraded warning only. After the first live run exposed a legitimate precision edge case (`expected_yes=0.007` while direct truth converged to flat zero), the observer was tightened to compare on normalized 2-decimal share precision and to reset its match streak after sync errors so stale cached truth cannot fake convergence. The harness path now waits on the same observer before returning `merge_succeeded`, which keeps `standard_pass` strict without letting `/positions` lag create a false failure. Added regressions for missing-row-as-flat handling, the two-consecutive-match requirement, sync-error streak reset, normalized sub-share residuals, non-blocking timeout warnings, and lagged harness convergence. Updated `<repo-root>/STRATEGY.md` Section 5 after finding it still described the old receipt-polling path and incorrectly implied `POLY_FUNDER` was required. Live validation on 2026-04-13: `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_merge_live_probe.ps1 -Scenario scotus_mail_ballots_pair_merge_probe` returned `meta_pass=true`, `standard_pass=true`, `cleanup_pass=true`, `pair_exit_status=merge_succeeded`, and `collateral_delta_usdc=3.000000`. Confidence this closes the observed merge/probe timing race without weakening the acceptance bar: **high**. Confidence the production runtime cost stays off the main discovery/quoting path because the extra wait lives only in a detached post-merge observer: **high**.
- 2026-04-13: fixed the shared hedge-resolution post-sync fallback so mixed buy-side `hedge + sellback` resolutions do not false-fail when the first retry proves only the hedge leg while sellback truth still lags. In `<repo-root>/src/runtime/live_engine.rs`, the sellback retry now remains available even after a hedge-evidence retry, and execution-confirmed sellbacks can project from the current post-sync position once hedge truth is already confirmed. Added regression coverage for the mixed-path projection helper and updated `<repo-root>/STRATEGY.md` Section 6.6 plus agent docs to describe this as shared fill-handler/reconciliation behavior. Confidence this closes the mixed-path false-failure hole in the uncommitted patch: **high**. Confidence the added runtime cost stays bounded to rare stale-truth resolution paths and does not affect normal discovery/quoting loops: **high**.
- 2026-04-12: fixed reconciliation’s delayed-fill hedge-resolution false halt when execution-confirmed sellbacks race with stale position truth. In `<repo-root>/src/runtime/live_engine.rs`, the post-resolution sync now performs one bounded retry when a sellback is verified but exposure still exceeds tolerance, and execution-confirmed sellback evidence can override a stale post-sync position even if a position snapshot exists. Added unit regressions for sellback-evidence retry gating. Updated `<repo-root>/STRATEGY.md` Section 6.6 to acknowledge that reconciliation still enforces exposure verification and can derive exposure from execution-confirmed sellbacks during truth lag. Confidence this addresses the observed false-halt path from the 2026-04-13 run: **high**. Confidence the change remains bounded to reconciliation/resolution flows and does not add main strategy overhead: **high**.
- 2026-04-11: narrowed the archive growth problem in [archive.rs](<repo-root>/src/persistence/archive.rs) without changing trading behavior. New decision archives now append compact one-line JSON records into a daily `data/archive/decisions/YYYYMMDD.jsonl` file instead of creating one pretty-printed JSON file per decision, which removes the tiny-file explosion in the hot archive path. Startup now also performs a best-effort retention prune for `decisions/` only, deleting legacy `.json` and new `.jsonl` decision archives older than 7 days while leaving `sessions/` untouched so replay/export behavior remains unchanged. Persistence unit coverage in [archive_tests.rs](<repo-root>/tests/unit/persistence/archive_tests.rs) now proves JSONL append behavior, old decision-file pruning, recent-file preservation, and session retention. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this is persistence hygiene only. Confidence this solves the current disk-growth issue with minimal production surface change: **high**. Confidence startup-only best-effort pruning avoids changing bot runtime behavior while still bounding archive growth: **high**.
- 2026-04-11: fixed the SAFE relayer signature encoding so live merge submissions stop returning `HTTP 400 Bad Request`, then re-ran the live merge probe to confirm a full gasless merge. In [ctf_merge.rs](<repo-root>/src/trading/ctf_merge.rs), SAFE transaction signing now follows the relayer client expectation: the SafeTx digest is signed with the EIP-191 prefix and the signature v-byte is normalized to `31/32` (v_raw + 31), matching the relayer’s SAFE submission format and the Polymarket relayer API example signature shape. This removed the 400s and the live probe reported `pair_exit_status=merge_succeeded`, a concrete `merge_tx_hash`, and a `+3.000000` USDC collateral delta with `cleanup_pass=true`. Validation: `cargo test ctf_merge_ -- --nocapture`, `python scripts/kill_flatten.py` to clear the baseline, and `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_merge_live_probe.ps1 -Scenario scotus_mail_ballots_pair_merge_probe` (green). Confidence the remaining merge path is now correctly wired to Polymarket’s SAFE relayer: **high**. Confidence the live probe now proves end-to-end merge execution (not just harness wiring): **high**.
- 2026-04-11: replaced the old self-funded Polygon-RPC CTF merge transport with a gasless SAFE relayer path and removed `POLYGON_RPC_URL` from merge code and merge preflight. In [ctf_merge.rs](<repo-root>/src/trading/ctf_merge.rs), `CtfMerger` now validates relayer auth through `RELAYER_API_KEY` + `RELAYER_API_KEY_ADDRESS`, checks SAFE deployment and SAFE nonce readiness, submits both the one-time `setApprovalForAll` transaction and the actual `mergePositions` transaction through Polymarket’s relayer, and polls relayer transaction state to terminal success/failure instead of broadcasting raw Polygon transactions. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), merger initialization now uses signer/funder credentials plus relayer env only, with no `POLYGON_RPC_URL` dependency. The dedicated merge-only live probe and terminology now treat relayer readiness as the acquisition gate, and the old RPC-shaped constructor tests were replaced with mocked relayer coverage in [ctf_merge_tests.rs](<repo-root>/tests/unit/trading/ctf_merge_tests.rs). Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): the strategy doc now correctly describes gasless SAFE relayer merge rather than self-funded Polygon submission. Confidence the merge transport is now aligned with issue #14’s economic-feasibility requirement from the bot wallet’s perspective: **high**. Confidence the runtime surface area stayed narrow and isolated to the existing `PairMerger` seam: **high**.
- 2026-04-09: tightened the merge validation seam after the first live merge-only probe uncovered two real operator issues: the sample scenario allowed a sub-`$1` marketable NO buy, and the merge transport was initializing without a healthy Polygon RPC because `POLYGON_RPC_URL` was unset and the default `https://polygon-rpc.com` fallback was returning handshake/HTML failures. In [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs), merge live-probe scenario validation now enforces the venue’s minimum marketable-buy notional on both acquisition legs, and merge probe failures now preserve the full error chain instead of only the top-level context. In [ctf_merge.rs](<repo-root>/src/trading/ctf_merge.rs), `PairMerger` gained a lightweight `preflight_check()` seam, `CtfMerger` now performs a harmless `eth_chainId` health check, and all Polygon JSON-RPC calls now surface HTTP / non-JSON response details instead of collapsing transport failures into opaque `error decoding response body`. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), the harness now exposes `harness_ctf_merge_preflight()` so the manual merge probe can fail before buying inventory when merge transport is dead. The dedicated merge probe now aborts safely before acquisition on the current local env because `POLYGON_RPC_URL` is missing and the default RPC endpoint is unhealthy, while cleanup still stays clean (`cleanup_pass=true`). This does not prove live merge works yet; it proves the current blocker is a merge-RPC configuration/transport issue, not missing merge code. Confidence the live merge blocker is currently environment/transport, not the pair-acquisition harness path: **high**. Confidence the new preflight removes unnecessary live-money risk from the manual merge probe without changing main-bot behavior: **high**.
- 2026-04-09: implemented issue #14’s validation seam without changing automatic production behavior. In [ctf_merge.rs](<repo-root>/src/trading/ctf_merge.rs), the concrete merger now sits behind a small `PairMerger` interface so deterministic harnesses can inject a mock external merge implementation. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), the existing post-fill pair-exit branch was extracted into shared helper methods (`try_merge_pairs`, `place_pair_fallback_asks`, `execute_pair_exit`) and exposed through two harness-only seams: `harness_ctf_merge_enabled()` for fast preflight and `harness_merge_pairs(...)` for a test-only/manual entrypoint that reuses the same production merge/fallback logic on authoritative current position truth. Deterministic Layer 1 now proves the extracted production merge-success branch is invoked and emits the expected merge-success telemetry via [merge_success_after_full_buy_hedge.json](<repo-root>/fixtures/hedge_scenarios/merge_success_after_full_buy_hedge.json) and the new `layer1_merge_success_redeems_paired_inventory` test, while the paired merge-failure/unconfigured fixtures were corrected to the current documented runtime behavior: because inventory asks only sell excess above a hedge pair, fully paired `YES==NO` inventory lands in `fallback_asks_failed` with zero tracked asks rather than `fallback_asks_placed`. This matches the existing known limitation already documented in [STRATEGY.md](<repo-root>/STRATEGY.md#L348), so no strategy update was required. On the live side, [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now includes a dedicated ignored/manual merge probe with its own scenario schema, env var, and runner path via [run_merge_live_probe.ps1](<repo-root>/scripts/run_merge_live_probe.ps1) plus [scotus_mail_ballots_pair_merge_probe.json](<repo-root>/fixtures/merge_live_probe_scenarios/scotus_mail_ballots_pair_merge_probe.json). That probe self-acquires a small YES/NO pair, verifies the pair through direct funded-wallet truth and synced engine truth, snapshots pre-exit collateral, invokes `harness_merge_pairs(...)`, requires `merge_succeeded` plus `merge_tx_hash` plus pre-cleanup pair disappearance and positive collateral delta, then still runs cleanup separately. Validation on April 9, 2026: `cargo test live_probe_ -- --nocapture`, `cargo test harness_merge_pairs_ -- --nocapture`, and `cargo test layer1_merge_ -- --nocapture` all passed. Confidence the deterministic harness proves the production merge-success branch itself rather than just constructor wiring: **high**. Confidence the dedicated live merge probe is the right high-signal manual check for issue #14 once a healthy Polygon RPC is configured: **high**.
- 2026-04-09: closed the remaining deterministic integration gap for the new BUY-resolution sellback recompute path by adding two Layer 2 replay scenarios in [raw_trade_sellback_miss_recompute_switches_to_hedge.json](<repo-root>/fixtures/hedge_replay_scenarios/raw_trade_sellback_miss_recompute_switches_to_hedge.json) and [raw_trade_sellback_miss_recompute_fails_closed.json](<repo-root>/fixtures/hedge_replay_scenarios/raw_trade_sellback_miss_recompute_fails_closed.json), plus new assertions in [layer2.rs](<repo-root>/tests/support/hedge/layer2.rs). The success replay now proves, end-to-end in the deterministic replay harness, that a raw-trade BUY resolution can: (1) place the first sellback as `SELL FOK` at the computed real `sellback_limit_price` (`0.73`, not `0.01`), (2) miss that first sellback, (3) sync authoritative residual truth, (4) recompute once, and (5) switch to a `BUY GTC` hedge at `0.27` on the residual. The negative replay proves the complementary bounded-failure path: two real-price sellback attempts at `0.73` and then fail-closed, followed by the already-existing kill/flatten safety sell at `0.01`; this is important because it shows the recompute itself is bounded to one retry even though the downstream safety path still emits its own flatten order. Validation on April 9, 2026: `cargo test layer2_sellback_miss_recompute_ -- --nocapture`, `cargo test layer2_ -- --nocapture`, `cargo test sellback_ -- --nocapture`, and `cargo test --workspace` all passed. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this increment only adds deterministic replay coverage and does not change production behavior. Confidence this closes the most important remaining integration gap for the sellback-miss recompute feature without adding runtime overhead: **high**. Confidence the negative replay’s extra `0.01` order is correctly understood as the pre-existing downstream kill/flatten safety path rather than an unbounded third recompute: **high**.
- 2026-04-09: aligned BUY-resolution sellback execution with the planner’s real `sellback_limit_price`, added one bounded recompute when that real-price `FOK` sellback misses, and then tightened Layer 3 to treat the resulting `execution_confirmed_sellback` / `sellback_complete` source as pre-cleanup confirmation for that same narrow production proof path. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), `execute_sellback_order(...)` now takes the caller-supplied sellback limit, the single-attempt resolution primitive remains unchanged, and a new narrow wrapper on `LiveEngine` runs exactly one extra authoritative sync plus one fresh `prepare_market_for_resolution(...)` / `plan_fill_resolution(...)` recompute for BUY-side resolutions whose first sellback attempt was not `VerifiedFilled`. If current synced residual exposure is already within `hedge_exposure_tolerance`, the run now completes from authoritative current truth with `post_sync_source="position_manager"` and no second order. If residual exposure still needs work, the runtime reruns the planner once on the residual position using fresh books/balance and the original trigger `fill_price`, then executes that recomputed plan once. If the second attempt still leaves unresolved exposure or missing truth, runtime fails closed as before. The legacy SELL-hedge `FOK @ $0.01` path in [hedge_executor.rs](<repo-root>/src/trading/hedge_executor.rs) remains unchanged in this increment. In [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs), Layer 3 now upgrades `production_sellback_confirmation_status=confirmed_before_cleanup` for the narrow case where production already emitted `post_sync_source="execution_confirmed_sellback"` with `exit_path_status="sellback_complete"` and neutral internal post-sync inventory before cleanup; this closes the last mismatch between the new production proof path and the live probe. Validation on April 9, 2026: `cargo fmt`, `cargo test sellback_ -- --nocapture`, `cargo test layer2_ -- --nocapture`, `cargo test live_probe_ -- --nocapture`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test --workspace`, and an armed SCOTUS rerun via `powershell -ExecutionPolicy Bypass -File <repo-root>/scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10` all passed. The final armed rerun came back green with `meta_pass=true`, `standard_pass=true`, `cleanup_pass=true`, `flow_status=sellback_completed`, `production_exit_path_status=sellback_complete`, `truth_reconciliation_status=confirmed`, `truth_reconciliation_warning_status=positions_lag_after_confirmed_execution`, and `production_sellback_confirmation_status=confirmed_before_cleanup`. Post-decision direct funded-wallet truth still briefly showed `YES=5.007056`, `NO=0` for `8s`, so the run now classifies that as lagging positions truth after confirmed execution instead of a production hedge failure. Cleanup still returned the wallet flat, so no `kill_flatten.py` run was required. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): the strategy doc now reflects that BUY-resolution sellbacks execute as real-price `FOK` orders with one bounded recompute on miss, while legacy SELL hedges still use `FOK @ $0.01`. Confidence this closes the planner-vs-executor sellback pricing divergence for BUY-resolution sellback only: **high**. Confidence the added runtime overhead stays bounded to the rare sellback-miss path and remains effectively zero on the main trading loop: **high**. Confidence the final green armed SCOTUS rerun is now a trustworthy signal for this covered sellback path at current Layer 3 scope: **high**.
- 2026-04-09: hardened the execution-confirmed sellback branch after the full-branch review so a green live-money Layer 3 result now depends on independent confirmation rather than production self-certification. In [client.rs](<repo-root>/src/trading/client.rs), authenticated order parsing now preserves the real `DELAYED` status instead of collapsing unknown statuses into `Invalid`, closing a sellback-lookup bug that could otherwise misclassify a legitimately pending order as verified zero fill. In [order.rs](<repo-root>/src/models/order.rs), associated trade IDs now live on `LiveOrder` itself instead of a process-global registry, removing the unbounded ambient state that the earlier review surfaced on the production path. In [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs), pre-cleanup sellback confirmation was tightened so Layer 3 no longer upgrades `confirmed_before_cleanup` from `post_sync_source=execution_confirmed_sellback` alone and no longer treats any positive matched shares as full sellback proof; confirmation now requires full independent sellback evidence or an already-flat post-decision funded-wallet snapshot. The ignored armed live-money test now also requires `cleanup_pass=true`, so the single green/red test signal reflects full strategy success plus cleanup hygiene. Validation on April 9, 2026: `cargo test live_probe_ -- --nocapture`, `cargo test sellback_ -- --nocapture`, `cargo test --workspace`, and a fresh armed rerun via `powershell -ExecutionPolicy Bypass -File scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10` all passed. The latest armed rerun was stronger than the earlier lag-warning pass: `meta_pass=true`, `standard_pass=true`, `cleanup_pass=true`, `truth_reconciliation_status=confirmed`, `truth_reconciliation_warning_status=null`, and post-decision direct funded-wallet truth was already flat for `9s` before cleanup, so `production_sellback_confirmation_status=confirmed_before_cleanup` now comes from independent flat-wallet evidence instead of a warning-path upgrade. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this pass only hardened execution proof and harness correctness. Confidence the review-exposed runtime/harness hazards are now closed: **high**. Confidence a full green armed SCOTUS rerun is now a trustworthy signal for the hedge stack at the current Layer 3 scope: **high**.
- 2026-04-09: implemented execution-confirmed sellback completion for the narrow sellback-only production path so the bot can finish successfully from authenticated execution evidence even when the positions endpoint still lags. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), sellback verification now tracks `confirmed_shares`, terminal placement success carries full requested-share confirmation forward, lookup confirmation requires full fill (`status=Matched` or `size_matched >= requested_sellback_shares`), and authenticated lookup trade IDs are preserved when available. When a resolution is sellback-only, sellback verification is `VerifiedFilled`, positions sync is still missing after the current sync/retry budget, and the runtime can derive a post-resolution position from `pre_resolution_position - confirmed_sellback_shares` whose residual exposure stays within `hedge_exposure_tolerance`, production now emits terminal success with `post_sync_source="execution_confirmed_sellback"` instead of failing closed on missing post-sync truth. BUY-hedge missing-truth behavior remains unchanged. The existing order client/model path was extended additively in [order.rs](<repo-root>/src/models/order.rs) and [client.rs](<repo-root>/src/trading/client.rs) to preserve `associate_trades` / `associated_trade_ids` from the existing authenticated order endpoint without adding new network calls or new hot-path APIs. Layer 3 in [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now treats `execution_confirmed_sellback` as a production-authoritative completion source for the same narrow sellback-only case, upgrades the pre-cleanup confirmation indicator from that source, and keeps one-sided positions lag as a warning rather than a hard contradiction when production already emitted `sellback_complete` from execution-confirmed proof. Replay fixtures in [raw_trade_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/raw_trade_sellback_cheaper.json) and [exchange_sync_missing_fill_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/exchange_sync_missing_fill_sellback_cheaper.json) were updated to assert the new success path. Validation on April 9, 2026: `cargo fmt`, `cargo test sellback_ -- --nocapture`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test fill_handler_fails_closed_when_post_sync_truth_remains_missing -- --nocapture`, `cargo test should_retry_resolution_sync -- --nocapture`, `cargo test layer2_ -- --nocapture`, `cargo test --workspace`, and an armed rerun via `powershell -ExecutionPolicy Bypass -File scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10` all passed. The armed rerun came back green with `meta_pass=true`, `standard_pass=true`, `cleanup_pass=true`, `flow_status=sellback_completed`, `production_exit_path_status=sellback_complete`, `truth_reconciliation_status=confirmed`, `truth_reconciliation_warning_status=positions_lag_after_confirmed_execution`, and `production_sellback_confirmation_status=confirmed_before_cleanup` while direct post-decision wallet truth still briefly showed `YES=5.007`, `NO=0` for 8 seconds before cleanup, which is exactly the lagging-positions case this increment was designed to classify correctly. Cleanup still returned the wallet flat, so no `kill_flatten.py` run was required. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this increment changes the proof model for sellback completion rather than hedge planning or execution intent. Confidence the production change now confirms the narrow sellback-only completion path from stronger execution evidence instead of missing positions truth: **high**. Confidence the armed SCOTUS rerun validates the intended end-to-end behavior for this increment: **high**. Confidence this adds no meaningful overhead outside the rare hedge-resolution path because it reuses existing placement/lookup behavior and local derivation only: **high**.
- 2026-04-08: narrowed the earlier rollback in [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) so production once again fails closed on missing final post-resolution truth instead of synthesizing flatness from `post_position=None`. `execute_resolution_plan(...)` now returns `success=false`, `post_sync_net_exposure=Decimal::MAX`, and the explicit failure reason `final post-sync position truth missing after current sync/retry flow` whenever truth is still missing after the current sync/retry budget. The broad synthetic-zero helper path was removed again, BUY-side retry-on-missing-truth was restored in `should_retry_resolution_sync(...)`, reconciliation baselines and `NeutralityEvaluated` now update only from authoritative `post_position`, and `emit_reconciliation_hedge_exit(...)` no longer re-reads cached position state to emit a terminal exit-path event after a missing-truth failure. The sellback-cheaper Layer 2 replay fixtures in [raw_trade_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/raw_trade_sellback_cheaper.json) and [exchange_sync_missing_fill_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/exchange_sync_missing_fill_sellback_cheaper.json) were tightened to assert the restored explicit failure reason, and the runtime tests now cover the no-exit-event stale-cache regression directly. Validation on April 8, 2026: `cargo fmt`, targeted missing-truth tests, `cargo test sellback_ -- --nocapture`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test layer2_ -- --nocapture`, and `cargo test --workspace` all passed. Armed reruns via `powershell -ExecutionPolicy Bypass -File scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10` confirmed the restored production behavior but did not preserve the earlier green Layer 3 outcome: the latest retained run log [events.jsonl](<repo-root>/data/events/run_20260409_035510/events.jsonl) shows `hedge_result_recorded` with `result_status="failed"`, `post_sync_source="first_sync"`, `post_sync_yes_size=null`, `post_sync_no_size=null`, `sellback_leg_status="success"`, and failure reason `final post-sync position truth missing after current sync/retry flow`, with no `hedge_exit_path_recorded` event emitted. Layer 3 therefore fell back to `truth_reconciliation_status=event_missing`, `production_exit_path_status=null`, and `standard_pass=false`, while cleanup still returned the funded wallet flat (`cleanup_pass=true`), so no `kill_flatten.py` run was needed. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this increment restores fail-closed runtime truth semantics rather than changing intended trading behavior. Confidence the false-neutral missing-truth regression is now removed again from production runtime state: **high**. Confidence the remaining live failure is now the expected consequence of fail-closed missing truth rather than another hidden false-neutral path: **high**.
- 2026-04-08: shifted the Layer 3 armed live-probe truth policy from “positions snapshot always wins” to “confirmed pre-cleanup execution wins, with a warning if positions truth lags” without changing production bot behavior. In [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs), Layer 3 now computes a dedicated `production_sellback_confirmation_status` / `production_sellback_confirmation_reason` before cleanup using existing production sellback evidence first, a bounded harness-side authenticated `get_order(sellback_order_id)` lookup second, and a final pre-cleanup direct-truth fallback when the funded wallet is already flat before `manual_live_probe_cleanup` begins. The old hard failure on “neutral internal exit event but directional post-decision direct truth” is now downgraded to `truth_reconciliation_warning_status=positions_lag_after_confirmed_execution` only when the contradiction is a one-sided positions-lag snapshot after a confirmed pre-cleanup `sellback_complete`; paired-inventory contradictions, unverified sellbacks, zero fills, and missing exit events still fail exactly as before. `flow_status` is normalized to `sellback_completed` for that same narrow case so `standard_pass` can stay true while `cleanup_pass` remains a separate mandatory verdict, and [TERMINOLOGY.md](<repo-root>/tests/support/hedge/TERMINOLOGY.md) now documents the warning contract and the new explicit “did production sell before cleanup?” indicator. Validation on April 8, 2026: `cargo fmt`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test resolve_sellback_confirmation_ -- --nocapture`, `cargo test classify_flow_status_ -- --nocapture`, `cargo test evaluate_standard_pass_ -- --nocapture`, `cargo test live_probe_ -- --nocapture`, `cargo test --workspace`, and armed reruns via `powershell -ExecutionPolicy Bypass -File scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10` all passed. The final armed rerun came back fully green with `standard_pass=true`, `cleanup_pass=true`, `flow_status=sellback_completed`, `truth_reconciliation_status=confirmed`, and `production_sellback_confirmation_status=confirmed_before_cleanup` because the post-decision funded-wallet snapshot was already flat before cleanup started, which proves production had already resolved the inventory. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this increment only changed harness-side truth weighting and adds zero performance overhead to the production trading bot. Confidence the remaining issue was harness truth weighting rather than production sellback execution: **high**. Confidence this harness-only warning/confirmation fix addresses the live contradiction without mutating prod behavior: **high**.
- 2026-04-08: surgically rolled back the production truth-path changes that were based on the wrong root-cause assumption, while keeping the strategy-alignment and observability work on this branch. After authenticated exchange-order verification on April 8, 2026 confirmed that the latest armed live run’s trigger BUY and sellback SELL both fully matched, the branch no longer treated short-window positions-endpoint lag as proof of a production hedge failure. [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) was restored away from the newer `FlatNoRow` / `retry_sync_no_row_flat` production path and back to the earlier zero-materialization semantics for successful authoritative sync sources, without undoing the kept sellback-verification tightening, additive hedge-result evidence, `tie_prefers_sellback` planner alignment, or Layer 3 repo-log retention fanout. The sellback-cheaper Layer 2 replay fixtures in [raw_trade_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/raw_trade_sellback_cheaper.json) and [exchange_sync_missing_fill_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/exchange_sync_missing_fill_sellback_cheaper.json) were reverted to match the restored production semantics, and [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) was backed away from the temporary “pre-exit missing-truth” failure wording that had been introduced to support the discarded production path. The one invalid full-flow test that still assumed a reachable successful dry-run sellback/no-row path was replaced with narrower helper coverage around `materialize_authoritative_zero_post_position(...)`, because the kept sellback-verification logic still correctly fails dry-run sellbacks closed as `unverified` when lookup evidence is missing. Validation on April 8, 2026: `cargo fmt`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test layer2_ -- --nocapture`, `cargo test fill_handler_ -- --nocapture`, `cargo test materialize_authoritative_zero_post_position -- --nocapture`, and `cargo test --workspace` all passed; the pre-existing unrelated `tests/unit/*` unused-import warnings remain. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document update was required because this increment removed a mistaken production truth interpretation rather than changing intended trading behavior. Confidence the selective rollback target was correct once authenticated order evidence showed prod sellback was successful: **high**. Confidence the remaining issue focus should now pivot to Layer 3 truth-weighting rather than more production hedge-behavior changes: **high**.
- 2026-04-08: implemented the narrow sellback-only post-resolution truth confirmation fix in [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) and validated it through unit, replay, workspace, and armed live-probe coverage. The runtime now classifies post-sync resolution truth explicitly as either a real market position or `FlatNoRow` after a successful positions sync with no market row. Sellback-only resolutions keep the existing fail-closed behavior by default, but they now get one bounded retry when the first successful post-sync result is `FlatNoRow` and the sellback leg is `VerifiedFilled`. If that retry also returns `FlatNoRow`, the runtime materializes an ephemeral zero [Position](<repo-root>/src/trading/positions.rs) only for resolution telemetry and exit classification, emitting `post_sync_source="retry_sync_no_row_flat"` through the existing `hedge_result_recorded` and `hedge_exit_path_recorded` events. BUY-hedge paths still do not adopt this no-row-flat shortcut, `PositionManager` storage semantics were not changed globally, and successful sync + no row outside the narrow verified sellback path now emits an explicit failure reason (`positions API returned no market row after post-resolution sync/retry flow`) instead of the older generic missing-truth wording. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) was updated only to recognize the new explicit no-row failure wording while continuing to fail any production/direct-truth contradiction. Layer 2 replay fixtures under [raw_trade_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/raw_trade_sellback_cheaper.json) and [exchange_sync_missing_fill_sellback_cheaper.json](<repo-root>/fixtures/hedge_replay_scenarios/exchange_sync_missing_fill_sellback_cheaper.json) were updated because the intended narrow no-row-flat path now resolves those scenarios successfully. Validation on April 8, 2026: `cargo fmt`, targeted runtime and Layer 3 tests (`cargo test resolution_truth_ -- --nocapture`, `cargo test finalize_resolution_post_sync -- --nocapture`, `cargo test sync_position_for_resolution_ -- --nocapture`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test reconciliation_exit_ -- --nocapture`, `cargo test fill_handler_fails_closed_when_post_sync_truth_remains_missing -- --nocapture`, `cargo test fill_handler_dry_run_sellback_only_missing_truth_halts_without_exit_event -- --nocapture`, `cargo test layer2_ -- --nocapture`), and `cargo test --workspace` all passed. Armed live rerun on April 8, 2026 via `powershell -ExecutionPolicy Bypass -File scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10` retained [events.jsonl](<repo-root>/data/events/run_20260409_001734/events.jsonl) and changed the live failure from generic missing truth to an explicit contradiction: production emitted `hedge_result_recorded` with `post_sync_source="retry_sync_no_row_flat"`, `post_sync_yes_size=0`, `post_sync_no_size=0`, then emitted `hedge_exit_path_recorded` with `exit_path_status="sellback_complete"`, while Layer 3 direct funded-wallet truth still observed `YES=5.007`, `NO=0` for `8s` before cleanup. Cleanup returned the market flat (`cleanup_pass=true`), so no extra `kill_flatten.py` step was required. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document change was required because the strategy still intends neutral post-resolution inventory; the new work only changed how sellback-only post-sync truth is classified and surfaced. Confidence the code change implemented the planned narrow sellback-only retry/no-row behavior correctly: **high**. Confidence the armed live rerun proves that `retry_sync_no_row_flat` is not currently authoritative enough to declare flat in production: **high**.
- 2026-04-07: fixed Layer 3 armed live-probe repo event log retention without changing production trading behavior. The armed harness in [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) was constructing a normal repo-backed `events.jsonl` writer through [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) and then immediately replacing `engine.event_producer` with an in-memory collector, which left the repo run directory created but empty for the exact live probe run under investigation. Added a test-only `FanoutEventProducer` in [support.rs](<repo-root>/tests/support/hedge/support.rs) and rewired the armed Layer 3 harness to fan out emitted runtime events to both the existing repo file producer and the in-memory collector instead of discarding the file producer. Added a regression in [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) proving a single emitted event now lands in both destinations. Validation: `cargo fmt`, `cargo test --bin spreadeater live_probe_event_fanout_preserves_repo_event_logs -- --nocapture`, then a real armed rerun on April 7, 2026 via `powershell -ExecutionPolicy Bypass -File scripts/run_hedge_live_probe.ps1 -Scenario scotus_mail_ballots_yes_bid_probe_under_10`. The new repo log [events.jsonl](<repo-root>/data/events/run_20260408_034030/events.jsonl) was retained at `6613` bytes instead of `0`. The retained event log materially strengthened the remaining production diagnosis: `hedge_result_recorded` shows `post_sync_source=\"first_sync\"`, `latency_ms=351`, `sellback_response_status=\"matched\"`, `sellback_order_id` populated, and still no `hedge_exit_path_recorded`, which raises confidence that the current sellback-only failure is a single immediate post-sellback truth-sync miss rather than a longer retry window expiring. Cleanup still returned the funded wallet flat (`cleanup_pass=true`), so no extra flatten step was required. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document change was required because this was test-only observability wiring and did not alter runtime trading behavior. Confidence the empty repo log root cause was harness-side event-producer replacement: **high**. Confidence the retained run log increases the “sellback-only path fails on first sync with no retry” diagnosis: **high**.
- 2026-04-07: tightened sellback completion semantics so the runtime no longer treats accepted sellback placement as sellback success. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), sellback execution now carries explicit internal verification state (`VerifiedFilled`, `VerifiedZeroFill`, `Unknown`) plus additive verification evidence. Placement is only terminal success when the sellback response is `Matched` or already carries non-empty `trade_ids`; `Invalid` is treated as verified zero fill; `Live` and `Delayed` now trigger exactly one bounded immediate `get_order(order_id)` lookup when an order id exists. Lookup `matched` or positive `size_matched` becomes verified fill, lookup `cancelled`/`invalid` with zero matched becomes verified zero fill, and `live`, `delayed`, `missing`, lookup errors, or provisional responses without an order id now fail closed as `unverified`. Aggregate hedge resolution success now requires both neutral post-sync exposure and verified sellback completion, and sellback-only dry-run paths now emit explicit sellback verification failures instead of placement-only `success`. [hedge.rs](<repo-root>/crates/spreadeater-core/src/payloads/hedge.rs) and [emitters.rs](<repo-root>/src/monitor/emitters.rs) were extended additively to surface `sellback_response_status`, `sellback_lookup_status`, `sellback_lookup_matched_shares`, `sellback_lookup_error`, and `sellback_trade_ids` on the existing `HedgeResultRecorded` payload with no schema bump. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now propagates that production sellback evidence and reports sellback `unverified` / verified-zero-fill as the primary production failure reason instead of burying them under the older generic missing-exit fallback. Validation: `cargo fmt`, targeted `cargo test sellback_ -- --nocapture`, targeted `cargo test reconcile_production_truth_ -- --nocapture`, then `cargo test --workspace`, all passing; existing unrelated `tests/unit/*` unused-import warnings remain. Reviewed against [STRATEGY.md](<repo-root>/STRATEGY.md): no strategy-document change was required for this increment because the runtime still uses the documented aggressive sellback `SELL FOK @ 0.01` path and this change only tightens completion verification semantics. Confidence the main production bug was placement-only sellback success attribution: **high**. Confidence this bounded-verify fix is the right first production increment without widening runtime scope: **high**.
- 2026-04-07: implemented the first two production hedge-fix increments uncovered by the Layer 3 harness and validated them end-to-end. In [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), missing final post-sync position truth is no longer materialized into a synthetic flat position for operational runtime state. The resolution path now reuses the existing one-retry sync budget when BUY-side hedge evidence is `VerifiedFilled` or `Unknown` but the first synced position is missing, and if truth is still missing after that current sync/retry flow it now fails closed with `success=false`, `post_sync_net_exposure=Decimal::MAX`, an explicit failure reason (`final post-sync position truth missing after current sync/retry flow`), and the existing market-halt path instead of emitting a false-neutral `no_exit_needed` outcome. This also removed operational synthetic-zero use from reconciliation baselines, neutrality emission, hedge-signal insertion, and exit classification. In [hedge_executor.rs](<repo-root>/src/trading/hedge_executor.rs), BUY-side tie handling now matches [STRATEGY.md](<repo-root>/STRATEGY.md): exact cost ties route to sellback, not hedge. [emitters.rs](<repo-root>/src/monitor/emitters.rs) and [TERMINOLOGY.md](<repo-root>/tests/support/hedge/TERMINOLOGY.md) were updated so the canonical decision reason is now `tie_prefers_sellback`. Replay/live-probe regressions were updated to the new semantics in [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) and the Layer 2 fixtures under `fixtures/hedge_replay_scenarios/`. Validation: targeted runtime/planner/live-probe tests passed and `cargo test --workspace` passed. Armed live validation on April 7, 2026 reached a real SCOTUS result with `meta_pass=true`, `decision_audit_status=confirmed`, `production_decision_reason_code=tie_prefers_sellback`, `planned_hedge_shares=0`, `planned_sellback_shares=5`, `cleanup_pass=true`, and no residual wallet inventory after cleanup. The remaining live production failure is now narrower and explicit: sellback-only production still ended with `production_exit_path_status=null` and `truth_reconciliation_status=failed` because the resolution failed closed on `final post-sync position truth missing after current sync/retry flow` before exit classification. Confidence the false-neutral path is now removed from production runtime state: **high**. Confidence the old BUY-side decision contradiction is fixed and the bot is now following the documented tie-to-sellback policy: **high**. Confidence the next production blocker is specifically post-sellback truth resolution on flat/no-row exchange responses, not a remaining Layer 3 observability gap: **high**.
- 2026-04-07: added a deferred strategy-alignment note at [STRATEGY-MD-MISALIGNMENTS-2026-04-07.md](<repo-root>/retired strategy-alignment note) documenting two known section 4 gaps in the current live implementation. The note records that the sellback planner computes `sellback_limit_price` but live execution still submits sellback as `0.01` FOK, and that the current 10-second guard is not enforced as a true fill-to-finish lifecycle timeout. It also records the current assessment that finding `#2` is the larger future fix because the correct solution likely needs staged sellback execution, bounded re-evaluation, and only then a terminal `0.01` fallback rather than a simple price substitution. Documentation-only change; no production code paths or runtime behavior changed. Confidence the note accurately captures the current code/spec gap: **high**. Confidence that finding `#2` is the larger deferred fix: **high**.
- 2026-04-06: fixed the PR `#31` Linux CI build break in the `Rust / build` job without changing production bot behavior. The failure came from the test-only hedge harness declaration in [live_engine.rs](<repo-root>/src/runtime/live_engine.rs): `#[path = "<repo-root>/../tests/support/hedge/mod.rs"]` was being resolved by rustc from the synthetic inline-module directory `src/runtime/live_engine/tests/`, which does not exist in the checkout, so Linux Actions failed with `couldn't read ... tests/support/hedge/mod.rs`. [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) now embeds the hedge harness test modules with `include!(concat!(env!("CARGO_MANIFEST_DIR"), ...))` under `#[cfg(test)]`, preserving the exact same harness files while removing the platform-fragile relative module path. Validation: `cargo test --lib --no-run`, `cargo test processed_trade_cache_prunes_expired_entries -- --nocapture`, and `cargo test --workspace` all passed locally. Confidence this is the root cause of the current CI failure: **high**. Confidence the fix is test-only and adds zero runtime overhead to the production trading bot: **high**.
- 2026-04-06: closed the remaining Layer 3 hedge `lookup_unavailable` observability gap by surfacing hedge executor verification evidence through the existing `hedge_result_recorded` event instead of asking Layer 3 to guess from a later empty lookup. [hedge_executor.rs](<repo-root>/src/trading/hedge_executor.rs) now retains the already-paid-for hedge BUY verification facts it observes on the live path: cancel outcome and reason, post-cancel `get_order` status and matched shares, lookup error text, and placement `trade_ids`. [hedge.rs](<repo-root>/crates/spreadeater-core/src/payloads/hedge.rs) and [emitters.rs](<repo-root>/src/monitor/emitters.rs) project those facts onto additive `HedgeResultPayload` fields without any new event type, schema bump, strategy branch, wait, websocket subscription, or REST call. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now prefers that production evidence before bounded external fallback and adds `production_hedge_cancel_status`, `production_hedge_cancel_reason`, `production_hedge_lookup_status`, `production_hedge_lookup_matched_shares`, `production_hedge_lookup_error`, and `production_hedge_trade_ids` to the Layer 3 result. Validation: focused serde/emitter/resolver tests passed, `cargo test --workspace` passed, and an armed SCOTUS rerun on April 6, 2026 reached a real result with `meta_pass=true` and no `lookup_unavailable`; the new live verdict is `hedge_verification_state=production_lookup_missing_after_cancel_unknown` with production evidence `cancel_status=rejected`, `cancel_reason=\"matched orders can't be canceled\"`, and `lookup_status=missing`. Confidence the old harness-side ambiguity is materially closed without meaningful main-bot overhead: **high**. Confidence the remaining failure is now a production-behavior issue, not a hedge-verification observability hole: **high**.
- 2026-04-06: fixed the uptime-sensitive `Instant` underflow that was crashing real Layer 3 hedge validation on low-uptime hosts. [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) no longer computes trade-cache or recent-synthetic-fill expiry windows via raw `Instant - Duration`; it now uses safe elapsed-age checks plus internal TTL helpers, preserving the same `24h` processed-trade TTL and `15s` synthetic-fill TTL without any startup/uptime panic path. [order_manager.rs](<repo-root>/src/trading/order_manager.rs) now applies the same safe age-check pattern to `cleanup_stale_cancels`, and the pending-cancel retry filter now uses the same non-panicking comparison style. Tests were made deterministic on fresh boots by removing touched raw `Instant::now() - Duration` fixtures and replacing them with small-TTL helper coverage plus a bounded real backoff wait in the pending-cancel retry test. Validation: targeted underflow regressions and the formerly failing Layer 0/2 hedge-harness cases all passed, then `cargo test --workspace` passed. Armed live validation on the SCOTUS scenario now reaches a real Layer 3 result instead of crashing: `meta_pass=true`, `production_exit_path_status=no_exit_needed`, but `standard_pass=false`, `cleanup_pass=false`, with direct truth showing residual inventory (`post_decision YES=5.007 NO=0`, later cleanup `YES=5.007 NO=5`) and `hedge_verification_state=lookup_unavailable`. Confidence the underflow blocker itself is fixed: **high**. Confidence the new live result is now a real hedge-test finding rather than a runtime crash artifact: **high**.
- 2026-04-06: completed a harness-only Layer 3 hedge observability cleanup pass without touching production trading code or [live_engine.rs](<repo-root>/src/runtime/live_engine.rs). [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now makes direct funded-wallet truth stage-specific and canonical by adding `post_decision_direct_*` and `cleanup_direct_*` fields, routing reconciliation and cleanup through one shared YES/NO summarization path, and explicitly labeling `truth_reconciliation_reason` as `stage=post_decision` and `cleanup_status` as `stage=cleanup`. The same file also replaces the old placeholder `hedge_verification_state` copy with a real Layer 3 verdict driven by existing bounded external evidence (`get_order` plus existing open-order visibility) and now preserves that evidence via `hedge_lookup_status` and `hedge_lookup_matched_shares`. [TERMINOLOGY.md](<repo-root>/tests/support/hedge/TERMINOLOGY.md) and [layer3-meta-pass-observability-report-2026-04-01.md](<repo-root>/agents/archive/hedge-test/layer3-meta-pass-observability-report-2026-04-01.md) were updated to document the new Layer 3 semantics and the current validation state. Validation: targeted Layer 3 tests passed (`cargo test resolve_hedge_verification_ -- --nocapture`, `cargo test reconcile_production_truth_ -- --nocapture`, `cargo test build_cleanup_status_ -- --nocapture`, `cargo test post_decision_and_cleanup_snapshots_ -- --nocapture`). `cargo test --workspace` and the armed SCOTUS rerun are currently blocked by a pre-existing `Instant` underflow path in [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) (`overflow when subtracting duration from instant`) that this pass intentionally did not modify. Confidence the non-production Layer 3 reporting gaps from the prior report are now closed or materially narrowed: **high**. Confidence the next blocker is outside this pass and must be fixed before fresh live confirmation: **high**.
- 2026-04-01: closed the remaining `hedge_exit_path_recorded` observability gap for successful hedge traces without changing production strategy behavior. [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) now rebuilds hedge-exit telemetry from the final known post-sync position instead of only the initial `result.post_position`, reuses that path in both fill-handler and reconciliation flows, and distinguishes between two very different cases: a successful sync with no position entry now materializes as authoritative zero inventory for exit reporting, while truly unrecoverable successful traces still emit an explicitly linked observability defect rather than silently skipping `hedge_exit_path_recorded`. This preserves the no-extra-I/O constraint for reconciliation by using cached position fallback only, while the fill-handler reuses its existing late sync rather than adding a new one. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now treats a successful `HedgeResultRecorded` without `HedgeExitPathRecorded` as an explicit `truth_reconciliation_status="failed"` observability defect instead of the old soft `event_missing` fallback; failed or legacy traces still keep `event_missing` fallback semantics. Added deterministic runtime coverage for late-sync backfill, successful zero-inventory exit emission, explicit unrecoverable observability failure, and reconciliation cached-position fallback in [live_engine.rs](<repo-root>/src/runtime/live_engine.rs), plus Layer 3 helper coverage in [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs). Updated [TERMINOLOGY.md](<repo-root>/tests/support/hedge/TERMINOLOGY.md) to make the successful-trace exit-event contract explicit. Validation on April 1, 2026: targeted `cargo test fill_handler_`, `cargo test reconciliation_exit_`, and `cargo test reconcile_production_truth_` passed; `cargo test --workspace` passed; the armed SCOTUS Layer 3 live rerun is currently blocked by a non-flat direct baseline on funded wallet `<redacted-wallet>` for condition `<redacted-id>` (`YES=5.007056`, `NO=5`), so live acceptance must be rerun only after the account is flattened again. Confidence the remaining hedge-exit gap is now closed in code for both successful runtime paths without adding strategy overhead: **high**. Confidence Layer 3 now fails missing success-path exit observability explicitly instead of masking it as a benign fallback: **high**.
- 2026-04-01: implemented the requested monitor-only hedge failure observability pass for Layer 3 without changing production trading behavior. [envelope.rs](<repo-root>/crates/spreadeater-core/src/envelope.rs) now uses schema `V1_5` and defines the explicit hedge lifecycle events `hedge_decision_evaluated` and `hedge_exit_path_recorded`, while [hedge.rs](<repo-root>/crates/spreadeater-core/src/payloads/hedge.rs) adds canonical payloads for production hedge-decision inputs/split/reasoning and post-sync exit-path follow-through. [emitters.rs](<repo-root>/src/monitor/emitters.rs) and [live_engine.rs](<repo-root>/src/runtime/live_engine.rs) now emit those events on both fill-handler and reconciliation hedge paths using only data already computed or already fetched in the existing runtime, so the main bot takes no new strategy branches and no extra production network I/O; the incremental runtime cost is limited to two extra event envelopes on rare hedge paths. [projector/mod.rs](<repo-root>/crates/spreadeater-monitor/src/projector/mod.rs), [store.rs](<repo-root>/crates/spreadeater-monitor/src/store.rs), and [postgres_integration.rs](<repo-root>/crates/spreadeater-monitor/tests/postgres_integration.rs) now accept/store/serialize the new event types and expose readable `hedge_decision` / `hedge_exit` trace statuses. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now prefers those explicit production events over reverse inference, adds `production_decision_mode`, `production_decision_reason_code`, `production_exit_path_status`, `merge_status`, `merge_failure_reason`, `fallback_status`, `fallback_failure_reason`, `truth_reconciliation_status`, and `truth_reconciliation_reason`, and fails `standard_pass` when production’s internal post-sync event truth clearly contradicts direct funded-wallet truth. [TERMINOLOGY.md](<repo-root>/tests/support/hedge/TERMINOLOGY.md) was updated so the canonical hedge vocabulary now matches the new event names and Layer 3 result schema. Validation: `cargo test --workspace` passed on 2026-04-01; the monitor Postgres integration scenarios compiled and remained ignored because they require a local Postgres instance. Confidence the pass satisfies the requested observability scope without introducing extra main-bot network overhead or trading-decision changes: **high**. Confidence Layer 3 now explains production hedge decision/exit failures materially better than the old inference-only path: **high**.
- 2026-04-01: tightened the hedge harness decision audit in Layers 2 and 3 without changing production runtime behavior. Layer 2 in [layer2.rs](<repo-root>/tests/support/hedge/layer2.rs) now proves `sellback vs hedge + split` outcomes through all four current pre-attribution ingress families that can lead to hedging: raw user-trade attribution, order-update fallback, exchange-sync missing-fill recovery, and orphan/reconciliation recovery. Added four new replay fixtures under `fixtures/hedge_replay_scenarios/` and upgraded the assertions to verify actual signed order-placement request logs, not just emitted payload fields, so sellback-only cases now prove “no hedge BUY request happened” and split cases now prove both the hedge BUY and sellback SELL requests with the expected sizes/prices. Layer 3 in [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now separates `standard_pass` from `cleanup_pass`, records explicit strategy-audit fields (`decision_audit_status`, `decision_audit_reason`, `planned_hedge_shares`, `planned_sellback_shares`, `hedge_leg_status`, `sellback_leg_status`) plus explicit follow-through fields (`flow_status`, `merge_observed`, `fallback_asks_observed`, `cleanup_status`), and treats live ambiguity conservatively: `standard_pass` now fails only on clear decision contradictions or missing required follow-through, while cleanup remains a separate verdict. Validation: `cargo test layer2_ -- --nocapture` (`10/10`), targeted live-probe verdict helper tests (`evaluate_standard_pass_*`, `evaluate_decision_audit_*`, `classify_flow_status_*`) all passing, then full `cargo test` passing (`189 passed; 0 failed; 2 ignored` in the primary runtime suite). Confidence deterministic Layer 2 branch-coverage is now in place across the intended ingress families: **high**. Confidence Layer 3 now expresses strategy correctness versus cleanup correctly without modifying production bot logic: **high**. Confidence the remaining live decision proof is correctly ambiguity-aware under the no-prod-change constraint: **high**.
- 2026-04-01: added a small hedge-harness terminology note at [TERMINOLOGY.md](<repo-root>/tests/support/hedge/TERMINOLOGY.md). It captures the currently agreed vocabulary for `meta-pass`, `standard-pass`, `cleanup-pass`, `precondition failure`, and `harness failure`, and it makes the key distinction explicit that `standard-pass` is about whether the bot did the right production thing for the scenario, while `cleanup-pass` is a separate probe-hygiene question. Confidence this matches the current intended terminology: **high**.
- 2026-04-01: hardened the Layer 3 post-cleanup verdict so the hedge harness now uses stable direct funded-wallet position truth before it can call a live probe clean. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now aggregates direct YES/NO per-condition positions from the public positions API for the actual funded wallet, compares cleanup truth against the exact baseline, and requires that baseline match to remain stable across a bounded observation window before `cleanup_status="clean"` and `standard_pass=true` are allowed. This was necessary because the first direct-truth patch was still too weak: a live SCOTUS rerun reported `cleanup_status="clean"` and `standard_pass=true`, but an immediate external check against the funded wallet still showed a mergeable pair on the target condition (`YES 5.007056`, `NO 5`). Added fast harness tests for the new direct-truth aggregation and exact-baseline matching logic: `cargo test --bin spreadeater summarize_direct_market_positions_aggregates_yes_and_no -- --nocapture` and `cargo test --bin spreadeater direct_market_position_truth_matches_requires_exact_baseline -- --nocapture`, both passing. Current live state after that rerun is still non-flat on the SCOTUS market (`YES 5.007056`, `NO 5` on proxy wallet `<redacted-wallet>`), so the next Layer 3 rerun must wait until the market is flattened again. Confidence the old one-sample cleanup verdict was unsafely optimistic: **high**. Confidence the new stable direct-truth requirement closes that false-pass hole without changing production behavior: **high**.
- 2026-04-01: tightened the manual Layer 3 hedge path to a `60s` budget and added a connect-only user-stream smoke path so live user-stream transport/auth can be checked without placing orders. [live_probe.rs](<repo-root>/tests/support/hedge/live_probe.rs) now defaults the probe timeout to `60s`, [scotus_mail_ballots_yes_bid_probe_under_10.json](<repo-root>/fixtures/hedge_live_probe_scenarios/scotus_mail_ballots_yes_bid_probe_under_10.json) was reduced from `180s` to `60s`, and [run_user_stream_smoke.ps1](<repo-root>/scripts/run_user_stream_smoke.ps1) was added for the connect-only ignored smoke test. Investigation outcome: Polymarket’s authenticated user websocket can stay silent for at least `60s` on an idle subscribed connection, so the Layer 3 probe no longer waits for a pre-order `Connected` ACK before placing the trigger. I also replaced the harness-only trigger seeding dependency on `sync_market_open_orders(...ObserveOnly)` with a `#[cfg(test)]` seeding helper in [order_manager.rs](<repo-root>/src/trading/order_manager.rs) so the probe can seed the placed trigger order into `OrderManager` without relying on the market-filtered `/data/orders?market=...` path that returned `401`. Validation/findings: `cargo run --quiet -- auth-check` passed and reported `open_orders=0`; the connect-only smoke reached transport connect + subscription send but received no first frame within `60s`; the repaired live probe then got past the websocket/auth gating and failed only on the clean-baseline guard, revealing a real residual `YES` position on the proxy wallet for the SCOTUS condition (`yes_size=5.007056`, `no_size=0`, condition `<redacted-id>`). Direct public lookup confirms that live inventory exists on `POLY_FUNDER` (`<redacted-wallet>`), so Layer 3 is currently blocked by live state rather than by the harness. Confidence the idle-ACK deadlock was a harness bug and is now removed without changing production bot behavior: **high**. Confidence the remaining blocker is real residual inventory on the live probe account: **high**.
- 2026-04-01: prepared a concrete operator-ready Layer 3 live probe for `https://polymarket.com/event/scotus-bars-counting-mail-ballots-after-election-day/scotus-bars-counting-mail-ballots-after-election-day` at [scotus_mail_ballots_yes_bid_probe_under_10.json](<repo-root>/fixtures/hedge_live_probe_scenarios/scotus_mail_ballots_yes_bid_probe_under_10.json). I resolved the live market metadata and books on April 1, 2026, confirmed the market is currently `tick_size=0.01`, `neg_risk=false`, and `min_order_size=5`, and set a 5-share YES trigger with conservative caps so bounded trigger + planned hedge + cleanup order notional stays under `$10` (`3.45 + 2.00 + 4.50 = 9.95` cap). I also updated [run_hedge_live_probe.ps1](<repo-root>/scripts/run_hedge_live_probe.ps1) so the manual Layer 3 wrapper automatically loads the repo `.env` before invoking the ignored live-money test. No live probe was executed yet. Confidence the prepared scenario is valid for this market as of April 1, 2026: **high**. Confidence the under-`$10` cap is correctly enforced by the scenario values themselves: **high**.
- 2026-04-01: rebuilt the hedge validation stack as a test-only harness on top of the restored production code without reintroducing any harness runtime hooks into the normal bot path. `src/runtime/live_engine.rs` now loads private-path hedge tests only under `#[cfg(test)]`, and the new support package under `tests/support/hedge/` provides shared scenario parsing, an in-process mock exchange, event capture, Layer 0 residual-sizing/dedupe/fallback unit coverage, Layer 1 post-attribution `FillHandler::handle_fill(...)` scenario coverage, Layer 2 pre-attribution/event-sequence replay coverage against the current private `LiveEngine` methods, and an ignored manual Layer 3 live-money probe test. Added `scripts/run_hedge_live_probe.ps1` as the operator wrapper for the live probe. Validation: `cargo test layer0_ -- --nocapture` (`3/3`), `cargo test layer1_ -- --nocapture` (`4/4`), `cargo test layer2_ -- --nocapture` (`6/6`), then `cargo test` (full default suite passing; the manual live probe remains ignored by design). Important limit retained by policy: a perfect offline full-engine meta-pass is still not possible without reintroducing production runtime hooks, so the ignored live-money Layer 3 probe is the only true meta-pass path. Confidence the new harness exercises the current production hedge behavior as far as possible without changing bot logic: **high**. Confidence the remaining offline-vs-live limitation is real and should not be papered over: **high**.
- 2026-04-01: reverted the production-facing portion of PR `#27` on `fix/revert-prod-changes-from-hedge-harness` by restoring the pre-merge versions of the shared runtime/trading files and removing the harness runtime/module/CLI wiring from the active build, while intentionally leaving the harness source/tests/fixtures on disk as reference material. This puts the live bot source path back on the last known good pre-harness production surface and avoids keeping the reverted `LiveEngine` test hooks on the hot path. To keep the repo green without pretending the old harness still fits restored production, I also reverted the PR `#27` unit-test deltas that depended on the reverted model/API surface and froze `tests/hedge_test_harness.rs`, `tests/hedge_replay_harness.rs`, and `tests/hedge_live_probe.rs` with `#![cfg(any())]`, preserving them as dormant reference crates rather than active default tests. Validation: `cargo test --bin spreadeater -- --nocapture` (`161/161` passing), then `cargo test` (full suite passing). Recommendation after the rollback: keep the Layer 1 fixtures/scenarios and harness code as salvage candidates, but rebuild Layer 2/3 from restored production instead of patching the reverted hooks back into prod. Confidence the production rollback was the right first move: **high**. Confidence "harness" is the better term for the preserved artifact here because the valuable part is the executable/scenario-driving scaffolding, not just the set of assertions: **high**. Confidence Layer 1 is worth triaging before discard while Layer 2/3 should restart from restored production: **high**.
- 2026-03-29: completed a focused harness correctness/maintainability pass without changing Layer 1/2 fidelity boundaries. Added `src/trading/trade_parser.rs` so REST trade lookups and user-stream trade messages now normalize through one shared parser for side/status/timestamp/event construction. Cleaned up `src/runtime/hedge_live_probe.rs` by replacing probe-truth `f64 -> Decimal` conversion with Decimal-aware JSON number/string deserialization and replacing the positional trigger-summary constructor with named `TriggerSummaryParts`. Added additive non-zero-fee coverage to `tests/hedge_test_harness.rs`, `tests/hedge_replay_harness.rs`, and `tests/hedge_live_probe.rs` so the mock suites exercise the real `/fee-rate` fetch/signing path under both non-zero defaults and per-token overrides while preserving existing zero-fee fixture defaults. Updated `HEDGE_TESTING_SUITE.md` to make the Layer 1 post-attribution vs Layer 2 pre/through-attribution fidelity boundary explicit. Validation: `cargo test --test hedge_test_harness -- --nocapture`, `cargo test --test hedge_replay_harness -- --nocapture`, `cargo test --test hedge_live_probe -- --nocapture`, `cargo test --bin spreadeater -- --nocapture`, and `cargo test`. Confidence the four targeted fixes are in without changing what Layer 1 and Layer 2 are supposed to prove: **high**.
- 2026-03-29: restored the shared BUY `FOK/FAK` safety rail in `src/trading/client.rs` and redesigned Layer 3 trigger acquisition so `hedge-live-probe` no longer depends on share-sized BUY `FOK`. The live probe now acquires its synthetic trigger with an aggressive share-sized BUY `GTC`, lets real matching user-stream trades hit the engine immediately, explicitly cancels any remainder after the bounded observation window, and only counts the trigger as successful when the normalized cumulative user-stream trigger shares exactly equal the requested trigger size. Added the client regression that share-sized BUY `FOK` is rejected again, switched the Layer 3 mock suite to `GTC + cancel`, and added exact-multi-trade and overshoot trigger regressions in `tests/hedge_live_probe.rs`. Revalidated with `cargo test --test hedge_live_probe -- --nocapture` (`23/23`), `cargo test --test hedge_test_harness -- --nocapture` (`8/8`), `cargo test --test hedge_replay_harness -- --nocapture` (`9/9`), `cargo test --bin spreadeater -- --nocapture` (`165/165`), and `cargo test` (full suite passing; pre-existing unrelated unused-import warnings remain in `tests/unit/*`). Confidence that Layer 3 now preserves the shared production safety model while keeping the trigger input path production-faithful: **high**.
- 2026-03-29: resumed and finished the interrupted Layer 3 production-mirror pass. `tests/hedge_live_probe.rs` now asserts the correct production-faithful contract: no-trigger/no-trade scenarios are standard failures but not meta-failures, and partial-trigger scenarios may still observe a production `result_status=success` while failing the scenario on a stricter expected bound. Revalidated the whole hedge stack with `cargo test --test hedge_live_probe -- --nocapture` (`21/21`), `cargo test --test hedge_test_harness -- --nocapture` (`8/8`), `cargo test --test hedge_replay_harness -- --nocapture` (`9/9`), `cargo test --bin spreadeater -- --nocapture` (`165/165`), and `cargo test` (full suite passing; pre-existing unrelated unused-import warnings remain in `tests/unit/*`). Confidence that the interrupted Layer 3 refactor is now internally consistent and regression-green: **high**.
- 2026-03-29: completed the Layer 3 user-stream parity refactor so `hedge-live-probe` now starts hedging from the same kind of live signal the bot uses: authenticated `UserEvent::Trade(...)` from the user stream, not REST-resolved trigger synthesis. `src/config.rs` now carries a configurable `discovery.user_ws_url`, `src/trading/user_stream.rs` now supports an injectable websocket URL with the production default preserved, `src/runtime/hedge_test.rs` now hosts a mock user websocket alongside the mock REST exchange, and `src/runtime/hedge_live_probe.rs` now subscribes to the real user stream before placing the bounded `FOK` trigger BUY, seeds the real trigger order into `OrderManager`, waits for matching user-stream trade/order events, and only then drives the existing replay helpers so the normal Layer 2/production path remains `UserEvent::Trade(...) -> build_fill_work_item(...) -> handle_fill(...)`. REST order/trade/position lookups are now diagnostics, cleanup, and final-flatness verification only. Added isolated no-money user-stream trigger tests plus delayed/duplicate/missing WS-trigger regressions in `tests/hedge_live_probe.rs`. Validated with `cargo check --quiet`, `cargo test --test hedge_live_probe -- --nocapture`, `cargo test --test hedge_test_harness -- --nocapture`, `cargo test --test hedge_replay_harness -- --nocapture`, `cargo test --bin spreadeater -- --nocapture`, and `cargo test`. Confidence that Layer 3 now matches the live bot’s hedge input path wherever feasible without adding meaningful main-bot overhead: **high**.
- 2026-03-29: finished the final Layer 3 trigger-path correction so `hedge-live-probe` now starts from the same kind of live signal as the real bot: execution evidence first, positions later. `src/runtime/hedge_live_probe.rs` now acquires the trigger leg with a bounded share-sized `FOK` BUY, keeps the richer live placement response (`status`, `trade_ids`, `transaction_hashes`, `taking_amount`, `making_amount`), resolves the actual live trade through `TradingClient::get_trades(...)`, seeds the acquired trigger order into `OrderManager`, and replays a real `UserEvent::Trade(...)` through the existing replay helpers so Layer 3 reuses `build_fill_work_item(...)` plus the normal downstream hedge path instead of manually constructing a `FillWorkItem`. Probe-local raw position/order truth is now used only for cleanup, final flatness verification, and classifying conservative failures when a trigger fill happened but could not be resolved into a replayable trade. Added mock `/data/trades` support plus richer order/trade models, rewrote `tests/hedge_live_probe.rs` around the new FOK + trade-resolution contract, and validated with `cargo check --quiet`, `cargo test --test hedge_live_probe -- --nocapture`, `cargo test --test hedge_test_harness -- --nocapture`, `cargo test --test hedge_replay_harness -- --nocapture`, `cargo test --bin spreadeater -- --nocapture`, and `cargo test`. Confidence this fixes Layer 3 without adding meaningful overhead to the main bot path: **high**.
- 2026-03-29: tightened Layer 3 so probe-only direct truth is a verification and cleanup gate, not a replacement hedge signal. `src/runtime/hedge_live_probe.rs` now reads the target market’s direct live position truth independently of the engine cache, but only uses that truth to decide whether to keep waiting for the engine view, whether cleanup must flatten probe-owned inventory, and whether final `clean_end_state=true` is actually safe to report. The probe still only enters the real hedge path after the engine confirms the full trigger fill, so hedge sizing and execution remain on the existing bot logic. If direct truth shows a trigger fill but the engine never catches up, the probe now flattens and fails conservatively instead of hedging from probe-only truth or falsely claiming the market is clean. Expanded `tests/hedge_live_probe.rs` with regressions for engine/direct disagreement, trigger-filled-but-engine-unconfirmed flatten failure, direct-truth partial fills, and dual-truth cleanup verification. Validated with `cargo test --test hedge_live_probe -- --nocapture`, `cargo test --test hedge_test_harness -- --nocapture`, `cargo test --test hedge_replay_harness -- --nocapture`, `cargo test --bin spreadeater -- --nocapture`, and `cargo test`. Confidence this preserves Layer 3 fidelity without adding meaningful overhead to the main bot path: **high**.
- 2026-03-29: addressed PR #28 review follow-ups on `watchdog-fix`. Removed the stray notification-check comment from `src/main.rs` and added a watchdog-manager regression that proves the enforcement state machine de-escalates from `KillPending` to `Warning` on degraded recovery, clears the pending kill, and only re-escalates after sustained degradation. Validated with `cargo test enforcing_watchdog_recovers_from_kill_pending_before_re_escalating -- --nocapture` and `cargo test watchdog -- --nocapture` (targeted watchdog suite passing).
- 2026-03-28: replaced the original Layer 3 synthetic live probe with a true paired live probe. `src/runtime/hedge_live_probe.rs` now acquires a small real trigger-side position first, derives a real post-attribution `FillWorkItem` from actual order lookup plus position delta, runs the real downstream `handle_fill(...)` path without touching `LiveEngine::run()`, and then performs probe-owned `merge_or_flatten` cleanup that must return the target market to a clean end state. Added harness-only post-hedge controls in `src/runtime/live_engine.rs` so the probe can reuse the real hedge path while suppressing probe-unsafe fallback asks, updated the live-probe templates under `fixtures/hedge_live_probe_scenarios/` to the new paired schema (`template_small_yes_buy_probe.json`, `template_small_no_buy_probe.json`, `scotus_mail_ballots_buy_probe_5.json`), expanded `tests/hedge_live_probe.rs` to cover arm gating, clean-market rejection, trigger price caps, partial trigger acquisition, merge cleanup, flatten cleanup, cleanup failure, and CLI non-zero behavior, and updated `HEDGE_TESTING_SUITE.md` plus the hedge report to document the paired-probe contract. Validated with `cargo test --test hedge_live_probe -- --nocapture`, `cargo test --test hedge_replay_harness -- --nocapture`, `cargo test --test hedge_test_harness -- --nocapture`, `cargo test --bin spreadeater -- --nocapture`, and `cargo test`. 556 unique tests passing (161 inline + 395 integration).
- 2026-03-28: implemented Phase 2 / Layer 2 hedge replay. Added `src/runtime/hedge_replay.rs` plus `hedge-replay --scenario <path>` in `src/main.rs`, with replay-only `LiveEngine` hooks that drive the real raw user-event attribution path, pending-fill fallback flushing, exchange-truth missed-fill detection, orphaned-position recovery, and reconciliation without changing normal `live` behavior. Added six deterministic fixtures under `fixtures/hedge_replay_scenarios/` and replay integration coverage in `tests/hedge_replay_harness.rs`, including the old “production fill-work-item sizing” concern through the real pre-attribution path. Validated with `cargo test --test hedge_replay_harness -- --nocapture`, `cargo test --test hedge_test_harness -- --nocapture`, `cargo test hedge_size_for_accounted_fill_ -- --nocapture`, manual `cargo run -- hedge-replay --scenario ...` checks, and a full `cargo test`. 542 unique tests passing (161 inline + 381 integration).
- 2026-03-28: corrected PR 27 into a true Layer 1 post-attribution hedge harness. `src/runtime/hedge_test.rs` now consumes an explicit `trigger.work_item` schema, uses a real `TradingClient` against a mutable in-process mock exchange, captures hedge/neutrality events plus final risk state for deterministic assertions, and fails on expectation mismatches instead of only runtime errors. Replaced the old fixtures with `clean_full_buy_hedge`, `thin_book_split`, `delayed_truth_confirmation`, and `resolution_failure_halts_market`; moved harness coverage into `tests/hedge_test_harness.rs`; added focused live-engine sizing regressions. Validated with `cargo test --test hedge_test_harness -- --nocapture`, `cargo test --bin spreadeater`, `cargo test`, and manual `cargo run -- hedge-test --scenario ...` checks. 533 tests passing (161 inline + 372 integration).
- 2026-03-28: fixed hedge timeout gap (Issue #23). Added 15s reqwest timeout to TradingClient HTTP client to prevent indefinite API hangs. Added `execute_resolution_plan_with_timeout` wrapper in live_engine.rs using `tokio::time::timeout` with `hedge_timeout_secs` (default 10s). Updated both hedge call sites (fill handler + reconciliation) to use the timeout wrapper. On timeout: mutex releases immediately, hedge marked failed, market killed by existing failure path. Added inline test for timeout behavior. 522 tests passing (158 inline + 364 integration).
- 2026-03-28: completed extensive unit test expansion. Added 237 new integration tests across 45 new test files in `tests/unit/`, covering models, config, strategy, risk, books, discovery, trading core, auth, persistence, reporting, and monitor modules. Added shared test helpers (`tests/unit/helpers.rs`) and `tempfile = "3"` dev-dependency for filesystem isolation. Total: 364 integration tests + 157 inline tests = 521 passing tests, all running in ~4 seconds. See `agents/archive/unit-tests/Extensive-unit-tests.md` for full phase breakdown.
- 2026-03-28: fixed the `feature/hedging-fix` CI/test break end-to-end. Added `src/lib.rs` so integration tests can import `spreadeater::...` from a real library target, restored `WatchdogConfig` serde-default behavior with `#[serde(default)]`, aligned the stale reconnect watchdog test with the current threshold semantics, and changed the watchdog-manager test path to inject a mock `KillAction` instead of the production `KillTrigger` so `cargo test` no longer exits the harness via `std::process::exit(1)`. This clears the Rust workflow’s `cargo build` / `cargo test` steps locally without changing normal CLI/runtime behavior. Validated with `cargo build --verbose` and `cargo test --verbose`. Manual validation not run.
- 2026-03-27: completed the Phase 3 Shai incident hedge-correctness pass. Raw user-trade handling in `live_engine.rs` no longer force-matches fills by `condition_id + asset_id + side` or synthesizes hedgeable tracked orders for unanchored trades; only exact `maker_order_id` / `taker_order_id` matches and the existing order-update pending-fill fallback can hedge immediately, while unanchored raw trades are logged/emitted as deferred to reconciliation. Added a bounded runtime `trade_id` dedupe cache, idempotent per-market halt handling that preserves the original halt reason, and targeted orphaned-position recovery on the existing quote-refresh cadence for one-sided positions with zero tracked buy commitment. Resolution execution now preserves the existing compare-both-sides planner but always executes planner-selected sell-back shares as aggressive `0.01` FOK exits to match `STRATEGY.md`, then evaluates success through a two-step position-truth ladder (`first_sync` / optional `retry_sync`) so a verified-or-unknown hedge BUY can be confirmed from actual opposite-side inventory growth before escalation. `FillDetected`, `HedgeIntentCreated`, and `HedgeResultRecorded` payloads gained additive audit fields for anchored-vs-deferred attribution, sell-back reference/execution prices, post-sync YES/NO sizes, post-sync source, and suppressed duplicate halt signals; schema version is now `1.4`. Added runtime regressions for processed-trade cache pruning, buy-hedge position-truth confirmation, retry-on-conflict post-sync logic, guaranteed-exit sell-back price, and duplicate halt suppression, plus shared payload/envelope serde coverage. Validated with `cargo test --bin spreadeater -- --skip watchdog::tests::enforcing_watchdog_emits_kill_triggered_after_confirmation` (156 passed). `cargo test --test core_types` still fails for the pre-existing unresolved `spreadeater` crate imports in `tests/unit/watchdog/*`; the new core payload/envelope fixtures themselves compile. Manual validation not run.
- 2026-03-27: added runtime cancel diagnostics and converted the watchdog to observe-only by default. `OrderCancelled` / `OrderResized` payloads now carry optional structured diagnostics for `quote_refresh_non_viable` and `hedge_depth` paths, including refresh viability reasons, effective quote size, available budget, hedgeable size, min size, and opposite-book top-of-book context. `check_hedge_depth()` now skips rounded no-op resizes instead of churning `same -> same` cancel-replace loops. Watchdog verdicts are now emitted into `events.jsonl` with enforcement state plus raw-vs-parsed book WS telemetry (`last_raw_book_ws_message_at`, `last_parsed_book_event_at`, `last_book_parse_error_at`, accepted/ignored/parse-error/snapshot/delta counters), while actual halt/kill actions are gated behind new config `watchdog.enforce_actions` and default disabled in checked-in config. Added watchdog event emission tests, cancel-diagnostics runtime tests, schema/projector compatibility updates, and monitor fixture updates. Validated with `cargo test --bin spreadeater` (152 passed) and `cargo test -p spreadeater-monitor --test postgres_integration --no-run`. Manual validation not run.
- 2026-03-27: completed the safety-first over-allocation hotfix for the March 27 live-entry incident. Global `sync_open_orders()` is now additive/update-only, so a partial `/data/orders` snapshot can no longer silently prune tracked bids or free budget. `detect_missed_fills_from_exchange()` now treats disappeared tracked bids conservatively: the first confirmed market-scoped miss keeps the order tracked and capital-reserved, reappearance clears suspicion, and only a second confirmed miss without corroborating position growth prunes the order. Fresh placement now honors the exchange-leg dedupe guard immediately, successful new/resize placements populate that guard immediately, and duplicate live BUY orders on the same `(condition_id, leg)` now route the market through the shared kill path instead of allowing repeated re-entry. Added regression coverage for non-destructive global sync, observe-only market sync, immediate exchange-leg reservation on placement/resize, first-miss retention, second-miss prune, reappearance clearing, and duplicate-live-leg kill. Validated with `cargo test --bin spreadeater` (144 passed). Manual validation not run.
- 2026-03-26: completed the Phase 2 March 24 incident fixes for hedge report points 1, 3, and 4. `refresh_quotes()` now runs a fast exchange-truth missed-fill detector on the existing refresh cadence: matched-size deltas from `/data/orders` and corroborated disappeared bid orders are synthesized straight into `FillHandler` with `match_source=exchange_order_sync`, while disappeared orders with no corroborating position delta are pruned without hedging. `prepare_market_for_resolution()` now starts with `OrderManager::sync_market_open_orders()` so stale tracked market bids are pruned before cancel-state counts and `available_hedge_resolution_usdc()` are computed. Reconciliation aggregate failures now go through the same shared halt/cancel/finalize/flatten flow on the first failure instead of waiting for repeated retries, and halted-market flattening now resolves metadata from `managed_markets` first and `known_markets` second while keeping markets halted/managed until post-sync exposure verifies within tolerance. Added routed runtime regressions for exchange-sync matched deltas, disappeared-order corroboration, stale-order pruning, stale-capital resolution prep, first-failure reconciliation halts, known-market flatten fallback, and flatten-verification retry retention. Validated with `cargo test --bin spreadeater` (137 passed). Manual validation not run.
- 2026-03-26: completed the Phase 1 hedge-resolution hardening pass. `plan_fill_resolution()` now enforces hedge affordability against the actual submitted BUY limit price, `prepare_market_for_resolution()` cancels all market orders concurrently and waits up to 2000ms with 100ms polls plus cancel retries before refreshing balance/books, and both FillHandler and reconciliation now use the same aggregate `execute_resolution_plan()` success rule (`post-sync net exposure <= tolerance`). Reconciliation baselines/asks now come only from the actual synced post-resolution position instead of the old synthetic patch. Hedge intent/result payloads gained planned split, cancel-window, and post-sync exposure fields for raw auditability. Added targeted planner/runtime/order-manager regressions and updated payload fixtures. Validated with `cargo test --bin spreadeater resolution_` (18 passed). Broader workspace validation is still blocked by unrelated root `core_types` watchdog import wiring and unrelated `spreadeater-monitor` watchdog-event exhaustiveness failures.
- 2026-03-24: replaced hardcoded $0.99 BUY hedge limit price with book-aware cost-benefit resolution. `compute_hedge_resolution()` walks both books per share (hedge cost vs sell-back cost), routes each share to the cheaper option, and applies an affordability gate. Both FillHandler and reconciliation paths now compute resolution from live book state. Motivated by the 2026-03-24 hedge incident where $0.99 × 373 = $369 was rejected on $97 balance — book-aware pricing at ~$0.27 would have been nearly affordable. Design doc at `agents/archive/hedge-incidents/hedge-incident-20260324/phase1-hedge-resolution-redesign.md`. 18 new tests, 127 total passing.
- 2026-04-11: added same-cycle frontier handoff to eliminate ~60-second idle-cash windows during frontier rotation. After cancelling the loser, the bot now polls for cancel verification within a configurable window (`frontier_handoff_window_secs`, default 5s). Once confirmed, it fresh-evaluates the current cycle's admitted/evaluated non-held markets with current books and budget, not the historical `known_markets` cache, and places the best viable market immediately — reducing the idle gap from ~60s to <5s. Falls back to next-cycle reservation if cancel is not verified in time. The Phase 3 `managed_markets` write-lock scope was also narrowed so polling/selection no longer blocks fill-handler metadata reads, and regression coverage now pins the disabled, timeout, success, and current-cycle-only selector cases. See GitHub #16. Confidence the same-cycle optimization now matches the intended frontier behavior without the lock contention issue: **high**.
- 2026-03-24: added frontier counterfactual swap observability without changing allocator behavior. `DecisionEvaluated` events now carry optional `frontier_counterfactual_*` fields for the selected entrant/loser pair, including the one-loser counterfactual budget, reclaimable bid capital, both condition IDs, both `reward_per_share` metrics, and both expected daily rewards. The runtime `Frontier rotation candidate selected` log now uses matching `frontier_counterfactual_*` labels so operator logs and raw event payloads reflect the actual swap rationale instead of the misleading immediate `would_trade=false` fallback rows. Validated with `cargo test --workspace`; the Docker-backed monitor Postgres suite could not be executed because the local Docker daemon was not running.
- 2026-03-23: fixed the strict frontier rotation capital leak. `LiveEngine` now arms a persistent frontier reservation when a loser market is canceled, freezes unrelated same-cycle new bid entries, skips loser-market bid maintenance while cancel verification is pending, and gives the reserved entrant first claim on the freed budget on the next discovery cycle before clearing the reservation. Added regression coverage for bid-only freeze behavior and reserved-entrant activation/clearance. Validated with `cargo test --workspace`.
- 2026-03-23: added a dedicated running investigation note for the current uncommitted allocator stack at `retired frontier-rotation investigation`. It captures live-run evidence for the ranking-only `reward_per_share` hotfix, the `min_outcome_price` outcome-mid fix, and the new strict frontier bid rotation layer, including confirmed yield deterioration on the held China market, proof that no `FRONTIER_REBALANCE` action fired in `run_20260324_023113`, and the current conclusion that suppressed residual-budget ranking rows are still misleading for operator judgment.
- 2026-03-23: added discovery-cycle strict bid rotation. The live bot now tracks continuous bid age on resting orders, preserves that age across same-market cancel-replace and resize paths, and runs a one-swap frontier rebalance on the 60s discovery cycle only. If a non-held market becomes better than an older held bid market when evaluated against that loser market's reclaimable bid capital, the bot cancels the loser market's bids with `FRONTIER_REBALANCE` and waits until the next discovery cycle to enter the winner. The 5s refresh path, asks, and held inventory remain unchanged. Added regression coverage for hold-window enforcement, better-market selection with reclaimed capital, worse-market rejection, pending-cancel suppression, and timestamp preservation.
- 2026-03-23: added watchdog module (`src/watchdog/`) with in-process WS health monitoring (silence detection, reconnect counting, status page polling) and 4-state escalation machine (Normal → Warning → KillPending → Killed). External Python sidecar (`scripts/watchdog_sidecar.py`) monitors heartbeat file for bot-crash safety net. Integrated into LiveEngine as independent tokio task.
- 2026-03-23: unified `min_outcome_price` enforcement on documented outcome-mid semantics. The bot no longer cancels imported or already-tracked bids solely because the resting order price is below the floor; it now relies on the existing book-backed checks that reject/cancel only when the outcome's own-book mid drops below the threshold. Added quote-engine and live-engine regressions to lock in the `0.19 bid with 0.20+ mid is allowed, but mid < floor still cancels` behavior.
- 2026-03-23: rolled back the aborted broad reward-yield refactor and replaced it with a minimal ranking-only hotfix. Viability/admission, hedgeability, monitor schema, and safety behavior stay on the pre-existing `return_per_share` path, while Phase 2 ordering now uses `reward_per_share = estimated_reward / hedge-aware shares` so favorable hedge economics no longer push low-yield markets to the top. Archived `DecisionEvaluated` ranking metadata and terminal `Market ranking` logs now use `reward_per_share` for the ordering metric only.
- 2026-03-23: added cycle-audit ranking fields to archived `DecisionEvaluated` events. Live discovery now emits decisions after sorting so each event carries `rank_in_cycle`, `ranked_market_count`, and the exact `return_per_share` used by the allocator. Terminal ranking logs now show that same metric, and the monitor-side DTO/store/web types preserve the new fields without DB changes.
- 2026-03-22: reverted viability/ranking denominator from per-dollar (price × size) back to per-share (size alone). In binary markets bid + hedge = $1/share, so shares = true account capacity. Matches `committed_exposure()` in order_manager.rs. Renamed `return_per_dollar` → `return_per_share` with serde alias. Status logs now show `¢/sh`. Tests rewritten for per-share model.
- 2026-03-22: fixed per-dollar estimation mismatches — status log `est_daily` now applies discount factor, market ranking uses `return_per_share` instead of absolute reward, viability denominator uses shares committed. Added regression tests against `compute_viability()`.
- 2026-03-22: redenominated reward estimation from per-share to per-dollar-committed. Status logs now show `r_per_dollar` (cents per dollar of committed capital) instead of `r_per_share`. The viability gate now applies `reward_discount_factor` (was previously only applied in logging — a bug). New `return_per_dollar` field on `RewardViability`. Decision rule changed from `P_Y + P_N - R < 100` to `R_dollar_effective > hedge_cost_per_dollar`. Framework doc rewritten as `agents/archive/per-share-ranking/Reward Per Dollar Estimation.md`.
- 2026-03-22: fixed the remaining false stale-book cancels by adding a narrow REST refresh-before-kill path for active bid markets. `check_hedge_depth()` now refreshes both YES/NO books concurrently with a 2s timeout when cached books are stale, uses those refreshed books in the same hedge-depth pass, and only risk-halts the market if the refresh fails. Archived `StatusSnapshot` events now also include the retained per-cycle book-WS counters so `events.jsonl` shows whether market-book traffic was being accepted, ignored, or failing to parse during live runs.
- 2026-03-22: updated the market websocket integration to the current Polymarket protocol. The bot now subscribes with the documented market-channel payload, parses inbound `event_type`, handles nested `price_change.price_changes` updates by `asset_id`, and logs per-cycle book-WS health counters (accepted, ignored, parse-error, snapshot, delta) so protocol drift is visible before stale-book kills recur.
- 2026-03-22: fixed the “enter then immediately cancel” loop on funded resting bids. Shared market evaluation now credits already-tracked BUY exposure back into dynamic sizing and bid placeability, so refresh and subsequent full cycles stop deadmitting a market solely because its own resting bid consumed the remaining free budget. Existing same-leg bids are preserved, incremental upsizes still require real free budget, and unrelated new bid legs remain suppressed when budget is exhausted.
- 2026-03-21: fixed the stale-book halt incident root cause. `check_hedge_depth()` now snapshots managed markets before any halt so stale-book kills cannot self-deadlock, cancel requests now return explicit verified/rejected/unknown outcomes, tracked orders are only removed after confirmed cancel, halted markets stay quarantined until active + pending cancels drain, and periodic cycle/refresh hooks retry pending cancels and finalize halted markets once safe.
- 2026-03-21: aligned live reward gating, refresh viability, and status estimation with actually placeable quotes. Live evaluation now suppresses unplaceable legs before scoring, refresh reuses the same cached-book viability path as admission, non-viable refreshes cancel bids instead of silently re-quoting them, status `est_daily` now uses the same score-proxy math and calibrated competition multiplier as selection, and the stale-book kill helper was restored on `LiveEngine` after the refactor.
- 2026-03-21: implemented all 9 STRATEGY.md deltas: quote refresh 5s, depth check 2s, hedge timeout 10s, budget from API balance minus $50 cash reserve, stale books kill market, per-market hedge mutex (replaces timestamp cooldowns), partial hedge sell-back, CTF merge as primary exit with fallback asks, merge only after hedge fully resolved.
- 2026-03-21: created `STRATEGY.md` — complete strategy breakdown for technical partner, covering all 8 strategy areas with 9 documented deltas (intended changes from current code) confirmed via step-by-step Q&A with the user.
- 2026-03-20: fixed four remaining fill/fallback correctness issues without changing hedge strategy. The fill handler now separates tracked-order accounting from hedge sizing, pending-fill fallback flushes use residual-based hedge sizing from live position state, BUY `GTC + cancel` hedges return explicit verification states (`VerifiedFilled` / `VerifiedZeroFill` / `Unknown`) instead of assuming missing lookups were filled, unknown hedges resolve against post-sync position truth, and recent-reconciliation duplicate suppression is now timestamp-aware so fresh fills are not dropped on balanced books.
- 2026-03-20: PR review fixes — GTC+cancel now verifies fill status via `get_order` (prevents silent zero-fill success), recon cooldown is position-aware (no longer blanket-suppresses genuine fills), `Connected` event deferred until server ACK, deduplicated `normalize_share_size` and `whole_share_budget_limit`, removed dead `_stale_fill_size` param and trivial `reconciliation_hedge_size` wrapper.
- 2026-03-20: merged the strategist engineer’s hedge-flow fix with the local safety additions. BUY hedges now use strategist-approved share-sized `GTC` at `0.99` with a short `500ms` grace period before canceling any remainder, hot-path and reconciliation hedges size from accounted residual exposure instead of raw WS events, reconnects sync positions again, unsafe share-sized BUY `FOK/FAK` requests are rejected structurally, `risk.hedge_exposure_tolerance` stays unified with a default of `0.5` shares, and the live passive bid gate now matches quote sizing by treating `available_budget` as the same whole-share hedge-aware exposure ceiling.
- 2026-03-11: main-first monitor reintegration completed on top of `origin/main`. The monitor workspace, shared event schema crate, app-side JSONL producer/writer path, v1.1 observability schema, and monitor Postgres/API/TUI/dashboard stack were ported back onto the current mainline trading/runtime code instead of preserving the old feature branch runtime verbatim.
- 2026-03-11: legacy `dry-run` / `dry-run-loop` orchestration now emits decision and quote/watchlist monitor telemetry only, while `live --dry-run` and `live` remain the full-pipeline monitored paths. Compatibility coverage was extended for the new schema fields and event types, including the dedicated Postgres monitor integration suite.
- 2026-03-11: monitor operator commands were simplified to direct `.cmd` entrypoints that only do `docker compose up -d`, print `http://127.0.0.1:8080`, and run the known-good `cargo run -p spreadeater-monitor -- serve --database-url ...` path without the earlier PowerShell background wrapper layer.

## Overview
Polymarket hedged liquidity rewards bot written in Rust. Discovers reward-eligible binary markets, places two-sided quotes (bids + asks) to earn liquidity rewards, and automatically hedges fills on the opposite outcome to remain market-neutral.

## Architecture

### Core Modules
- **`src/auth/`** - API credentials (env vars), HMAC-SHA256 L2 request signing, EIP-712 order signing
- **`src/books/`** - REST order book bootstrap, WebSocket delta maintenance (with auto-reconnect), in-memory BookManager
- **`src/config.rs`** - Layered config with defaults (strategy, risk, discovery, books, persistence)
- **`src/discovery/`** - REST client for Polymarket sampling-markets API, filter/reconcile pipeline
- **`src/models/`** - Market, OrderBook, Quote, Hedge, Decision, Order, Position, Events structs
- **`src/persistence/`** - File archive (JSON decision reports)
- **`src/reporting/`** - Shadow mode decision reports, CSV export, logging
- **`src/runtime/`** - Shadow orchestrator, LiveEngine (full live trading), ReplayEngine
- **`src/strategy/`** - Quote engine (4-leg), hedgeability depth-walk, reward viability, score proxy, calibration
- **`src/trading/`** - TradingClient, PositionManager, UserStream (WS), RiskManager, OrderManager, HedgeExecutor
- **`src/watchdog/`** - In-process health monitoring: `WsHealthTracker` (book/user WS silence detection, reconnect counting), `StatusPoller` (Polymarket Instatus page polling every 30s), `KillTrigger` (emergency kill+flatten via `kill_flatten.py` with process exit fallback), `WatchdogManager` (4-state escalation: Normal → Warning → KillPending → Killed). Hybrid two-layer design with external Python sidecar (`scripts/watchdog_sidecar.py`) monitoring heartbeat file for bot-crash safety net. Configured via `WatchdogConfig` (13 parameters). Spawned as independent tokio task in LiveEngine.

### Key Flows
1. **Discovery cycle** (every 60s): fetch sampling-markets, filter by reward threshold, evaluate all markets, rank viable markets by `reward_per_share = estimated_reward / hedge-aware committed shares` (tie-break by estimated daily reward), place/update quotes in priority order
2. **Quote refresh** (every N seconds): re-read cached books, cancel-replace drifted orders
3. **Hedge depth check** (every 15s): verify opposite-side liquidity supports resting bids, scale down if needed
4. **Fill handling** (real-time, dedicated task): UserStream WebSocket detects fills → forwarded to FillHandler task via channel → HedgeExecutor fires an exact-share hedge immediately (aggressive `0.99` `GTC` BUYs with `500ms` grace before canceling any remainder; aggressive SELL flattening), never blocked by periodic cycle work
5. **Hedge reconciliation** (every cycle): detects unhedged inventory from missed fills (offline/WS gaps), auto-hedges
5. **Risk management**: per-market halt, global halt, hedge timeout tracking, exposure limits

### CLI Commands
`once`, `run`, `show-config`, `auth-check`, `dry-run`, `dry-run-loop`, `live`, `export`, `replay`, `hedge-test`, `hedge-replay`, `hedge-live-probe`

## Tech Stack
Rust 1.94.0, tokio, reqwest, serde, tokio-tungstenite, chrono, clap, rust_decimal, tracing, hmac/sha2, k256/sha3 (EIP-712)

## Test Coverage (as of 2026-03-29)
- **562 unique passing tests** (401 integration + 161 inline), 0 failures
- **45 test files** in `tests/unit/` covering: models (position, order, orderbook, quote, market, decision, events, hedge, enriched), config, strategy (calibration, hedgeability, viability, score proxy, quote engine, reward estimation), books (manager, websocket), discovery (filters), trading (order manager, hedge executor, client, positions, CTF merge, user stream, risk), auth (credentials, signer, order signer), persistence (archive), reporting (shadow, CSV export), monitor (emitters, producer), watchdog (health, status poller, manager), and core (envelope, payload, reason codes)
- **Runtime:** ~4 seconds for all integration tests
- **Dev-dependencies:** `tempfile = "3"` for filesystem test isolation

## Current Status
- Stages 1-4 complete (shadow, live trading, hedging, score proxy refinement)
- Live mode operational with fill detection, exact-share hedging, conditional WS re-subscription
- Layer 1 hedge harness (`hedge-test`) covers deterministic post-attribution resolution behavior; Layer 2 hedge replay (`hedge-replay`) covers the real pre-attribution event path, pending fallback, exchange-sync missed-fill detection, and reconciliation/orphan recovery with deterministic fixtures; Layer 3 live probe (`hedge-live-probe`) now acquires a real trigger-side fill but starts hedging only from the authenticated user-stream trade signal, then reuses the normal `UserEvent::Trade(...) -> build_fill_work_item(...) -> handle_fill(...)` path under explicit arm/safety gates and probe-owned clean-end-state enforcement
- Fill handling runs on a dedicated tokio task (FillHandler) — hedges fire instantly, never blocked by cycle/depth/refresh work
- Fee rate caching eliminates REST round-trip from hedge critical path (5-min TTL)
- Hedge reconciliation catches missed fills from downtime/restarts across ALL known markets (not just admitted); cancel-replace race condition fixed
- Live admission viability is gated on `min_return_pct` using score-proxy reward minus estimated hedge cost; reward scoring now uses only actually placeable approved legs, and status `est_daily` is computed from the same score-proxy path over current resting orders
- Phase 2 ranking is narrower than viability: viable markets are ordered by `reward_per_share` (discounted reward over hedge-aware shares committed), so favorable hedge economics remain audit data but no longer increase ranking priority
- Re-evaluation now treats same-leg resting BUY orders as already funded, so quote refresh preserves existing bids under exhausted free budget instead of immediately deadmitting them
- Score proxy aligned with status log estimation (simple sums, no two_sided_q_min)
- cancel_replace_if_drifted now detects size drift (>50%), resizes oversized orders from prior runs
- Hedge-aware budget: available_budget uses full notional (bid + hedge reserve = $1/share) not just bid cost; prevents entering unhedgeable positions
- Balance fetched once per cycle from GET /balance-allowance, cached in Arc<RwLock> shared with FillHandler (was 3 API calls/cycle)
- RiskManager pre_trade_check includes balance gate for BUY hedges; skips position cap for hedge orders (hedges reduce net exposure); FillHandler warns when balance is tight
- Hedge depth check cancels bids when opposite book is missing or stale (lenient 120s threshold to avoid bid churn)
- HedgeExecutor uses aggressive share-sized `GTC` BUY hedges with `500ms` grace before canceling any remainder and aggressive `FOK` SELL hedges, preventing BUY market-order semantic mistakes while still crossing the spread fast
- Post-hedge asks use API-verified position (not synthetic expected_pos) — only places asks if both sides have inventory
- Post-hedge verification: hot-path hedges, reconciliation hedges, balance correction, and risk timeout logic all use the same configured residual tolerance (`risk.hedge_exposure_tolerance`)
- Hedge failure sell-back: when FOK hedge fails, attempts to sell the original fill at market before killing the market (prevents stranded directional positions)
- Kill switch flattening: kill_market() now halts immediately, retries/verify cancels until no orders remain live, then sells only unhedged excess inventory (directional only, not balanced hedged portion) before removing the market from the managed set
- Balance refreshed after each hedge in FillHandler — prevents stale risk checks during rapid-fire fills
- Position drift detection each cycle: cancels phantom asks (no inventory), warns on untracked positions (external trades)
- Ask placement falls back to REST book fetch when WS cache is empty (prevents silent failures overnight)
- Markets dropping below reward threshold preserve asks when holding inventory (cancel bids only)
- Viable markets with inventory but no tracked asks get explicit ask placement each cycle
- ANSI-colored banners: blue=fill, green=hedge OK, red=hedge fail, cyan=ask placed
- UserStream WS subscribes to existing orders before initial cycle (no fill-detection gap on startup)
- User-stream connect/reconnect status is observable and reconnect now triggers a position sync before trading continues
- Per-leg min_outcome_price filter ($0.20): skips bids on cheap outcomes, hedging into cheap side still allowed
- `min_outcome_price` is enforced on outcome-mid semantics end-to-end: entry and retention use the outcome's own-book mid (or the existing one-sided proxy in quote generation), not the resting bid price itself
- Market profitability ranking: viable markets sorted by `reward_per_share` before budget allocation, with estimated daily reward as the secondary tie-breaker
- Configurable ask depth (`ask_depth_pct`): offsets inventory asks from mid for trading PnL (0.0=at mid, 1.0=max_spread edge, default 0.20)
- Book WebSocket now connected with production-grade reliability: exponential backoff reconnection, 30s keepalive ping, auto-resubscription on market set changes
- evaluate_market() uses WS-maintained cached books (cache-first with REST fallback on stale/missing), reducing ~1,200 REST calls/hour for 10 markets
- WS Cancellation events use grace buffer (not hard-delete) to prevent fill→hedge race condition
- TrackedOrder stores `opposite_token_id` — FillHandler hedges even when market leaves managed set
- WebSocket reconnection uses exponential backoff (1s initial, max 30s) instead of fixed 5s delay
- Order signer rounds amounts per py-clob-client SDK (size: 2dp, price: tick_dp, cross product: tick_dp+2) — prevents 400 errors on reconciliation hedges
- User-stream observability now emits reconnect plus pending-fill-fallback queue/flush status events, making WS-gap vs fallback-path diagnosis visible in the event log
- Cancel semantics are conservative: ambiguous or rejected cancel attempts remain tracked and enter a retry path instead of silently disappearing from local state
- config.json supports `//` comments; reorganized by trade lifecycle with inline docs
- 2026-03-11 monitor ops expansion landed on top of the merged monitor stack:
  - monitor API now exposes dedicated paginated `open-orders`, `inventory`, `watchlist`, `history`, `errors`, and `config` endpoints
  - monitor tails a bot log file into Postgres with websocket `errors` broadcasts, so runtime errors are visible in the dashboard without adding new hot-path bot events
  - web monitor is now a tabbed operator console with dense searchable/filterable tables and a compact overview page
  - root `config.json` is strict JSON again; human guidance moved into `CONFIG.md`
